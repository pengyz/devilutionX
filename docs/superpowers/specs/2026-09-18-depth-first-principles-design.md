# 深度层规格：D1 体验的第一性重定（承重结构 · 存档契约 · 四个兑现）

**状态**：**已批准 v11**（2026-09-18：A 腿 Item 1 经独立复核后修正——`MAGICRES` 改名、补 `ALLRES`、**排除 `*_CURSE`**、**G2 反转为"可机械导出"**、G1 软化并改论据）
**历史状态**：已批准 v10（2026-09-18：清掉 §4.8 两项 ⚠——**elixir 存在** ✓（我此前误判 ✗）、空 id 行已解释 ✓；新增 **C5 elixir 可获得性**并挂到"缺失种类 #2"）
**历史状态**：已批准 v9（2026-09-18：作者"内容扩充关键在于是否有效服务体验"⇒ 新增**准入判据三条**、**§4.8 内容使能件 C1–C4**，并补 §1.2 三处事实）
**历史状态**：已批准 v8（2026-09-18：作者"回到主线"⇒ 新增 **§4.7 主线行动方案（四个失败点→四条腿）**，并把 §4.5 降为候选登记簿）
**历史状态**：已批准 v7（2026-09-18：v6 §1.2＋附录 C 获批；随后作者否决"耐久度"概念 ⇒ 新增**原则 5**（消耗必须是"可选择的支出"而非"必须的维护"）并把 D6 替换为 **D6′ 购买阶梯**）
**历史状态**：已批准 v6
**历史状态**：已批准 v5（2026-09-18：作者一问引入**预先规避检验**并据此将 **D1 标为待重定义**）
**历史状态**：已批准 v4（2026-09-18：v2 §4.1/§4.2、v3 §1.1/§4.5 均获作者批准；v4 由第一性对抗复核驱动，修订顺序与证据标准，**D5 放弃**、**D4→D4′**）→ **实施中**（仅第 0 章先行；§4.3 各自单独立项）。计划：`docs/superpowers/plans/2026-09-18-depth-save-contract.md`
**分类**：**Depth**（限制信息、重塑压力、取舍；非数值膨胀；无开关 ⇒ 默认生效）
**上位文档**：宪章 `2026-07-27-better-d1-design-charter.md`；已实施的内容密度契约 `2026-09-16-content-density-contract-design.md`；作者的存档政策决策 34/35（**不保证存档兼容**）

**本规格的边界（v2 收严）**：只写**契约点、失败语义、可观测结果**。
- **不指定函数、不指定测试实现** ✗（那属实现立项）。
- v1 曾越界指定"单机调用 `pfile_update(true)`"，**经核验该指定是错的**：`pfile_update()` 在单机首行即 `return`（`Source/pfile.cpp:828-830` ✓）⇒ 该方案在单机是**空操作** ✗。v1 的对应内容已全部降级（见附录 B：候选链，非规格内容）。

---

## 1. 问题陈述

**D1 的体验是一个自洽整体**（六件互相咬合）：两腔循环（城镇＝安全/经济/永久 ↔ 地牢＝危险/发现/**单程**）／单位＝一次**有限、持久、不可逆**的下降／信息未知但可学／准备有限且**在战斗内消耗**／低数量高杀伤的威胁／终局**合上**。

**业界缺少直接示例的原因**：没有人深化 D1 的设计，大家换成了 D2 的设计（无限、可重置、数值成长的跑步机）。

**判定**：D1 的"薄"**主要是实现面**，外加**两处设计面缺口**：

| D1 的薄 | 归类 | 核验锚点 |
|---|---|---|
| 高难度＝**HP×3 纯倍率** | 实现面 | `Source/monster.cpp:255` ✓ |
| 怪物**无群体行为** | 实现面（我们已开工） | `Source/monster.cpp:3054 PlaceGroup`／`:318 GetUniqueMonstPosition` ✓ |
| 主题房存在但**无控制** | 实现面 | `Source/levels/themes.cpp:349 PlaceThemeMonsts`／`:913 CreateThemeRooms` ✓ |
| 物品模型薄（无独立词缀字段） | 实现面 | `Source/items.h:204 _iMagical` ✓ |
| 信息/UI 停在 1996（已在补） | 实现面 | `Source/levels/gendung.cpp:327 CanTarget` ✓ |
| **① 存档不能兑现"不可逆"** | **设计面** | 见 §3/§4.1 |
| **② 信息/威胁只有单向** | **设计面（待裁决）** | 见 §4.2 |

### 1.1 设计层闭环审计（六结构 + 第七结构 × 两阶段）

**方法**：每个结构在前中期/后期各问四问——**有状态？有决策？有后果？有反制/恢复？** 四问全「有」才算**闭环**。

| 结构 | 前中期 | 后期 | 判定 |
|---|---|---|---|
| 两腔循环（城镇↔地牢） | ✓✓✓✓ | **金币不再是决策**（出口＝买/修/丢/取 `stores.cpp:2062/1482`、`control_gold.cpp:26`、`qol/stash.cpp:688` ✓；另有神殿出口 ⚠ 未核验） | 后期**未闭环** |
| 准备有限（腰带/卷轴/补给） | ✓✓✓✓ | **约束不再咬合**（`stores.cpp` 的 `HealPlayer` 提供免费治疗 ✓） | 后期**未闭环** |
| 信息未知但可学 | ✓✓✓✓ | ✓ | 闭环 |
| 单程下降 + 层持久 | ✓✓✓✓ | ✓（`diablo.cpp:3163` ✓） | 闭环 |
| 低数量高杀伤的威胁 | ✓✓✓✓ | 难度＝**纯倍率**（`monster.cpp:255` ✓）；**是否真被角色成长压过待实测** ⚠ | **待实测** |
| 终局合上 | — | ✓（**故意**如此） | 闭环 |
| **⑦ 持续消耗（本审计新增）** | ✓（补给 + 死亡代价） | **✗ 两者同时失效，且无第三种消耗替代** | **结构缺失** |

**结论（经两路独立复核修正 v3）**：缺口**不是三个并列项**，而是**一个**——**缺少"持续消耗"结构**（症状＝①金币失去决策作用 ②补给约束失效 ③威胁被成长压过）。
**且三者的共同地基是"死亡可撤销"**（单机死亡不写档 ✓；死亡时 ESC 一键读旧档 `diablo.cpp:524-532` ✓）⇒ **第 0 章（存档契约）是其前置条件**，而非并列项 ✓。

**反例检验（v4，来自第一性复核 + 我的实地核验）**：
1. **"D1 是空循环"被推翻** ✗ —— 地下城**确有花金场合**：神殿取**携带金的一半**换经验（`objects.cpp:2868` ✓）、Murphy 神殿取**三分之一**（`objects.cpp:2983` ✓ + `InitEMSG_SHRINE_MURPHYS` ✓）⇒ "带现金下潜"**有动机**（是**弱循环**，不是空循环 ✓）；
2. **但第七结构缺失的核心仍成立** ✓ —— 死亡不可逆被撤销 ⇒ 上述消耗与死亡代价**都可被回滚** ✗（症状未消失）；
3. **"第 0 章先行"仍是推论、不是证明** ✗ —— 见 §4.6 的**反例检验要求**。

### 1.2 资源体系与压力梯度（第一性；作者方法：变量→性质→收支→梯度→判定）

#### (1) 核心变量

| 变量 | 锚点 | 性质 |
|---|---|---|
| 生命 / 法力 | `_pHitPoints`/`_pMana` | 可恢复（药水/神殿/Pepin）；**无自然回复** ✓（`player.cpp` 的 HP/MP 自增均为 Mana Shield/吸血类 ✓）；单次下潜内单调递减 |
| **金币** | `_pGold` | **可转移**（丢 `control_gold.cpp:24-29` ✓／存 stash `qol/stash.cpp:685-690` ✓）；**可失**（死亡掉一半 `player.cpp:354-361` ✓） |
| 装备/物品 | `InvBody`/`InvList`/`SpdList` | 可转移/可丢/可卖；死亡掉落（`StartPlayerKill` ✓） |
| **背包 / 腰带** | `InventoryGridCells = 40`、`MaxBeltItems = 8` ✓（`player.h:38-39` ✓） | **约束**（非资源）：能带多少 / 战斗中能消耗多少 |
| **经验** | `_pExperience` | **不可转移** ✓；**不可囤积**（阈值夹取 `player.cpp:1428-1429` ✓）；当前**只有收入**（支出仅神殿 ✓） |
| 等级/属性/技能 | `_pLevel`/`_pBase*` | 单调、**不可逆**（未见洗点入口 ⚠） |
| **已知信息** | `DungeonFlag::Explored` ✓、`_pLvlVisited` ✓ | **不可转移、不可失**（持久 ✓） |
| **世界状态** | 门/尸体/地面掉落/已清场 | **不可转移**、持久 ✓（层不重生成 `diablo.cpp:3163` ✓） |
| **装备门槛**（v9 补） | `assets/txtdata/items/itemdat.tsv` 有 `minStrength`/`minMagic`/`minDexterity` 列 ✓ | **已有**"带得动/用得动"的门槛 ⇒ 做"深度化门槛"**无需新概念** ✓ |
| **词缀池**（v9 补） | `item_prefixes.tsv`（84 行 ✓）、`item_suffixes.tsv`（96 行 ✓）；含 `light`/`radiance`/`the dark`/`the night`/抗性系 ✓ | **内容已在**；缺的是"**需求耦合**"（§4.8 C1） |
| **升级恢复**（v9 补） | `NextPlrLevel`：`_pHitPoints = _pMaxHP`、`_pMaxMana = _pMaxMana` ✓ | **条件化满恢复**（进展驱动的泄压阀）⇒ 压力梯度分析须计入 ✓ |

#### (2) 资源性质（四问决定"什么设计可能"）

| 性质 | 谁具备 | **推论** |
|---|---|---|
| **P1 可转移/可丢弃** | 金币、装备、药水 | 成本**可被预先规避** ✗ ⇒ **不能承载稳定取舍**（红线 11） |
| **P2 战斗中已被消耗** | **药水（喝掉即不可回收）** | **不可规避** ✓✓ —— **唯一"战斗内天然硬消耗"** |
| **P3 有上限/夹取** | 经验、背包/腰带 | 经验**不可囤积** ⇒ 支出＝**推迟升级**（非消费存款） |
| **P4 不可转移** | 经验、知识、世界状态、时间 | **稳定压力的唯一落点** ✓✓ |
| **P5 可逆性** | 物质损失在**第 0 章前全部可回滚** ✗ | **第 0 章＝使物质损失重新有意义的开关** ✓ |

#### (3) 收入 / 支出 / 成本收益

| 资源 | 收入 | 支出 | 获取成本 | 收益 |
|---|---|---|---|---|
| HP/MP | 药水、神殿、Pepin（免费 ✓） | 战斗、法术 | 药水＝金币＋格子 | 生存 |
| **金币** | 掉落、卖装备、神殿 | 城内购买（`stores.cpp:2471/2658/2776` 均**限城内** ✓）、修理（`:1482` ✓）、地牢神殿取金（`objects.cpp:2868` 半金 ✓／`:2983` 三分之一 ✓） | 时间/风险/格子 | **通用兑换媒介** |
| **药水** | 掉落、购买 | **战斗内喝掉** | 金币＋格子（腰带 8 ✓） | 生存；**唯一不可规避消耗** |
| 装备 | 掉落/购买 | 卖/丢/**死亡掉落** | 金币/运气 | 战力；**死亡暴露** |
| **经验** | 击杀/任务 | **仅神殿**（Glowing 5% XP→属性 ✓，≤5000 归零 ✓；反向 Mendicant 金→XP ✓） | 时间/风险 | 升级（**自动** ✓） |
| 知识 | 玩家记忆/地图 | **不可支出** ✗ | **暴露风险** | 降低未知 |
| 世界状态 | —（被改变） | — | — | 持久后果（第 0 章激活 ✓） |

#### (4) 压力梯度（"缺口"的机理表述）

> **单次下潜压力 ＝ 需求（层威胁 × 每次交战消耗） ÷ 存量（药水携带 + 装备强度）；调节阀＝金币↔药水兑换率 与 回城成本。**

衰减机理（从性质推出，非感觉）：①战力↑ ⇒ 每次交战消耗↓（需求端衰减 ✗）②掉落价值↑ 而**金币出口集中城内且不随深度增长** ✓ ⇒ 供给过剩 ✗ ③回城卷轴便宜 ⇒ 压力被**"多跑一趟"**吸收 ✗（＝跑腿，DP3 ✗）；药水可**预先多带**（40 格）✗ ④第 0 章前物质损失可回滚 ⇒ 赌注失效 ✗。

#### (5) 判定与由此推出的原则

**结构自洽 ✓**（每条资源有源/汇；药水＝唯一战斗内硬消耗；金币＝唯一通用兑换媒介；经验＝唯一单调进度；知识/世界状态＝唯一不可逆资产）；
**强度不闭合 ✗**（(4) 的四条）。

**原则（由性质推出，非选方案）**：
1. 压力必须挂在**不可规避**类别（战斗内已消耗品 ✓ 或不可转移量 ✓）；
2. 强度随深度递增，且加在**需求端**（不是供给端）；
3. **不得用"多跑一趟"吸收压力**（若动补给通道，必须"每次使用即消耗"，囤积无效）；
4. 物质赌注须先有**不可逆**前提（第 0 章）；
5. **消耗必须是"可选择的支出"，不是"必须的维护"** ✓（2026-09-18 由作者对"耐久度"的判断推出）：以"衰减 → 必须补充"形态出现的成本（耐久/损耗/保养）**不是取舍** ✗ —— 它的内容只是**跑腿与付费**（红线 11 ✗），可用备用件规避（预先规避检验 ✗），且若需要"永不磨损"之类属性来对冲，则该机制本身是设计异味的信号 ✓。
   **判据（沿用旧问）**：拿掉它，玩家少的**是决策**还是**只是少跑几趟**？后者 ⇒ 不采纳 ✗。

**候选闭合形状**：①**战斗内消耗的深度化**（复用名册/小队机器 ✓，P2 ✓，需求端 ✓）；②**补给通道成本深度化**（关闭绕行通道，DP3 ✓）；③**XP→情报**（P3/P4 ✓）；④**世界状态作为不可逆后果**（第 0 章 + D3 ✓）；⑤**耐久/修理的深度化 + 金币容量**（见附录 C，由 D2 对照推出 ✓）。

## 2. 分类判定

| 步 | 判定 | 结果 |
|---|---|---|
| 第一步 | 不膨胀数值 | 不属 Expansion 数值膨胀 ✓ |
| 支柱 | 限制信息／重塑压力／取舍 | **Depth** ✓ |
| 无开关原则 | **单一契约**，不提供"硬核模式开关" | 默认生效 ✓ |
| 禁令 4 | 复用既有概念（存档、死亡、掉落、尸体、持久层） | ✓ |

## 3. 事实基础（**每条带 `文件:行号` 或用例名；未核验者显式标注**）

### 3.1 写档时机（v2 新增：v1 完全遗漏，且 v1 的假设是错的）

| 时机 | 路径 | 单机／多人 |
|---|---|---|
| **切层** | `Source/interfac.cpp:338/354/374/390/406/420/436/451`（`WM_DIABNEXTLVL` 等） | **单机：`pfile_save_level()`**（保存**关卡数据** ✓）；多人：`DeltaSaveLevel()` ✓ |
| **退出清理** | `Source/init.cpp:116-118` | **仅多人**（`if (gbIsMultiplayer && gbRunGame)` ✓） |
| **进度类事件** | `Source/diablo.cpp:865-866` | **仅多人**（`if (gbIsMultiplayer) pfile_write_hero();` ✓） |
| **定时/强制保存** | `Source/pfile.cpp:828-840 pfile_update(bool forceSave)` | **仅多人**：函数首行 `if (!gbIsMultiplayer) return;` ✓✓（⇒ **v1 的单机方案是空操作** ✗） |
| **手动保存** | `Source/gamemenu.cpp:353 SaveGame()` | 单机玩家的**唯一完整写档**（含英雄） ✓ |
| HF 转档 | `Source/loadsave.cpp:2702 SaveGame()` | 版本迁移时 ✓ |
| 多人物品/死亡强制保存 | `Source/msg.cpp:1624/1630/1651/1657`、自身死亡 `2079` | **仅多人**（codex 引用，**本会话未逐行复核** ⚠） |

**结论**：单机在**切层时保存关卡数据** ✓，但**英雄的完整写档只有手动 `SaveGame()`** ✓；**死亡不触发任何保存** ✗ ⇒ **单机玩家"死亡后直接退出并重开"即可撤销死亡** ✗（比读档更简单）。**这才是第 0 章要补的缺口**——且它比 v1 描述的更大（不是"一次写档"，而是"单机缺少死亡提交"）。

### 3.2 死亡语义与代价

| 事实 | 锚点 |
|---|---|
| 死亡例程 | `Source/player.cpp:2672 StartPlayerKill`；签名仅 2 参（`player.h:970` ✓），`dropItems`/`dropGold` 是**函数内局部量** ✓（v1 误称"参数" ✗） |
| 单机：被怪/陷阱杀死 ⇒ **掉落装备+掉金币** | `dropGold = !gbIsMultiplayer \|\| !(onLevel16 \|\| arena)`、`dropItems = dropGold && deathReason == MonsterOrTrap`；`&player == MyPlayer` 分支经 `DeadItem()` 掉落 ✓（⇒ **单机本来就有死亡掉落** ✓，v1 误称"需采纳多人规则" ✗） |
| PvP（玩家杀）只掉"耳朵"，且已有耳时不再掉 | `player.cpp:2685`、`:2736-2764` ✓（codex 引用，**未逐行复核** ⚠） |
| 多人 L16/竞技场例外不掉金/物 | `player.cpp:2683` ✓（**单机无此例外** ✓） |
| 死亡**不终结角色**：复活即清除死亡态 | `MyPlayerIsDead`（`player.h:925`；`player.cpp:1058` 置位、`:2528` 清除）✓ |
| 死亡态**可被完整表示** | `SavePlayer` 写入 `_pmode`（`loadsave.cpp:1272` ✓）与 HP 系列（`:1379-1382` ✓）；掉落物入 game data ⚠（codex 引用） |
| 层持久 | `Source/diablo.cpp:3163`（`!myPlayer._pLvlVisited[currlevel]` 分支）✓ |

### 3.3 写档的持久化弱点（v2 新增：二值契约的鲁棒性前提）

| 事实 | 锚点 |
|---|---|
| MPQ 写入**先删同名项再写**，失败仅 `LogError` 并 `return false` | `Source/mpq/mpq_writer.cpp:154`（删除在 `:167` 附近 ✓） |
| **调用方丢弃写返回值** | `Source/loadsave.cpp:249-255 SaveHelper::~SaveHelper` 调用 `WriteFile(...)` 且**不使用返回值** ✓ |
| UNPACKED 路径**截断写**（`"wb"`）后 `fwrite`，失败可留**半档/空档** | `Source/pfile.cpp:583-596 SaveWriter::WriteFile` ✓ |

## 4. 方案

### 4.1 第 0 章：存档契约（**契约层**：只定契约点、语义、失败语义）

> **存档＝提交（commit） / 时间线唯一 / 死亡留痕 / 提交失败不得提供死前 Load**

| 条 | 内容（可观测语义，不指定函数） |
|---|---|
| **① 死亡提交** | 单机玩家死亡后，**受管存档中必须已存在"死亡态"**：掉落物在死亡地点、金币已扣、角色处于死亡/复活流程的持久状态。⇒ 读档/退出重开**不能**回到死前 ✗ |
| **② 切层提交** | 进入/离开关卡时的持久化语义与多人一致（单机已有 `pfile_save_level` ✓，其语义须明确为"包含当层进展"） |
| **③ 时间线唯一** | **不提供**任何回滚到已提交死亡态的入口 ✗（无开关 ✓） |
| **④ 死亡不终结角色** | 死亡后可复活继续 ✓；"留痕"＝**装备/金币损失 + 尸体位置**，**不是删档** ✓（`MyPlayerIsDead` 复活即清除 ✓） |
| **⑤ 提交失败的语义（硬要求）** | 提交必须"**先持久化死亡意图，再原子替换完整存档**"；**中断/失败后启动时若发现未完成的死亡意图 ⇒ 禁止加载旧档**（进入死亡态或新局）✓；**不得**出现"死亡已发生但旧档仍可加载" ✗ |
| **⑥ 边界** | 契约只约束**应用内受管存档路径** ✓；**外部复制/替换存档文件不受约束**（显式声明 ✓） |
| **⑦ 多人** | **无变化** ✓（其写档与死亡语义已存在 ✓） |
| **⑧ 旧档** | 存活的旧档可继续使用 ✓；新语义**从下一次死亡起**适用 ✓；升级前未提交的死亡**不可追溯** ✓；**不做格式迁移** ✓（引用决策 34/35 ✓）+ 一次性契约提示 ✓ |

**为什么不是"新增规则"**：死亡掉落在**单机本来就有** ✓，多人**本来就写档** ✓ ⇒ 本契约 = **把单机补成与多人同一语义 + 给提交加原子性**，零新概念 ✓。

### 4.2 缺口 ②：信息/威胁的双向性（**只登记，待裁决**）

**v2 把判据改为可判定**（v1 的"一局内可学、不记数字"不可判定 ✗）：
> 判据：该交互必须能用**一句"如果 X 则 Y"**描述，且 **X、Y 都是既有 UI 元素或既有概念**；并且**必须做对照实验**（对照法见试玩协议：同种子 A/B + 强制选择）。
> **反例警戒**：光照压制当年也被认为"可行动"，最终因**无反制**被撤销（宪章 §6 记录）⇒ "直观可学"**不足以**避免 DP2 违规 ✗。

### 4.3 实现轨路线图（**只给方向、成本、依赖、与既有守卫的关系**；实现各自立项）

> **与 §4.5 的关系**：本节＝**实现轨**（怪物行为/房间控制/信息/UI 等实现短板）；§4.5＝**设计轨**（闭合"持续消耗"结构）。两轨的**共同前置都是第 0 章** ✓。

| # | 条目 | 成本 | 依赖 | 与既有守卫的关系 | 优先级 |
|---|---|---|---|---|---|
| ② | **守卫圣所**（持久空间的取舍：进不进这个房间） | 中 | 小队机器（已有 ✓） | **必须全量跑**采样守卫（约束数量/预算/确定性与 leader 行为） | **最省，先做** ✓ |
| ④ | **威胁模型的实现**（群体行为/威胁可读性） | 中 | 小队/拴系（已有 ✓） | **必须全量跑**同一批守卫 | 锚点最实，与 ② 并行 ✓ |
| ① | **情报可挣得** | 小-中 | 需先设计**代价机制** | 基本无关 | **未给代价机制前不做** ✗（否则退化为愿望，宪章已删过三篇同类文档 ✓） |
| ③ | **成对神殿**（地点的赌注） | 中-大 | 独立，但受**全层段定义**约束 | 至少不得破坏采样守卫 | 中期 |
| ⑤ | **终局：有限朝圣链** | **最大** | 横跨任务/层段/持久/结局状态 | 不得破坏 | 最后 |

### 4.4 明确排除

新系统（装备/socket/技能树/meta 进度）✗；无限或循环终局 ✗；背包/战利品稀缺税 ✗；roguelike（每局重置 + 局外成长）✗；**任何新增数值倍率** ✗（含"更高难度＝更高倍率"的延续 ✓ 现状即为反例 `monster.cpp:255`）。

### 4.5 设计轨候选登记簿（机制池；**顺序与取舍以 §4.7 主线为准** ✓）

| # | 闭合手段 | 取舍（红线 11） | 指名反制（红线 10） | 落地锚点（✓＝已核验） | 成本 | 验收要点 | 反证（撤销条件） | 状态 |
|---|---|---|---|---|---|---|---|---|
| **D1** ⚠ **待重定义** | **死亡赋税**（第 0 章生效后成为活循环）——**但已被"预先规避检验"打回** ✗：金牌成本可"先丢地上再捡回"规避 ⇒ 三选一：**(a)** 记为**弱效应**并降优先级 ✓；**(b)** 改挂**不可转移维度（经验）**——**D1 内生先例已存在**：神殿扣 5% 经验 `objects.cpp:2851` ✓、归零 `:2853` ✓ ⇒ 不可规避 ✓ 但须过**预注册实验**（防"惩罚过重"）✓；**(c)** 撤下 D1 ✗ | 带多少**现金**下潜 vs 存进 stash | 回城卷轴离场 / 存钱 | `DropHalfPlayersGold`（`player.cpp:354-361` ✓）；stash 银行（`qol/stash.cpp:685-690` ✓）；**地下城花金场合**：神殿半金换经验 `objects.cpp:2868` ✓、Murphy 三分之一 `:2983` ✓ | **零新机制** | 死亡后重载 ⇒ 携带金确实减半**且不可回滚**；**另须实测"神殿权重"**（出现频率 × 收益 ⚠） | ①实测"死后仍 100% 全额带现金下潜" ⇒ 空转；②神殿使用率≈0 ⇒ 无动机 | ✅ 可做（**先测权重**） |
| **D2** | **威胁换种类而非换数值** | 该层要带什么装备/药 | 掩体 / 贴身 / 引开 leader（已有 ✓） | 我们的名册/小队/压力机器 ✓；**先实测**后期是否真被压过（timedemo） | 中 | 逐层 × 职业的形态表（红线 14）+ 采样守卫全量 | 实测无感或仍被数值压倒 | ⚠ 先实测 |
| **D3** | **深层房间结构**（恢复 L16/Nest/Crypt 主题房 + roster core + 拴系小队） | 进不进这个房间 | 杀 leader / 卡门位 / 放弃房间 | `themes.cpp:913-915`（现**跳过** ✓）+ `PlaceGroup`（`monster.cpp:3054` ✓） | 中-大 | 每层 1-24 有定义、深层非零、重载后已清守卫不重生 | 变成重复刷房或职业失衡（红线 9/10/11/14） | ✅ 锚点已核验 |
| **D4′** | **补给压力＝按深度引导既有"箱子陷阱偷药"**（**不是**新增怪物 AI ✗） | 满腰带续航 vs 承担被窃风险 | 杀掠夺者（既有） / 少带高阶药 / 先开箱 | 既有路径 `objects.cpp:2057`（陷阱选择 `MissileID::StealPotions` ✓）+ 注册 `misdat.cpp:194` ✓；**但数据行只在 HF**（`mods/hf/txtdata/missiles/misdat.tsv:84` ✓）⇒ 怪物 AI 路线**需新接线** ✗（原 D4 作废） | 中 | 只影响 `SpdList`、背包不受影响、每层上限、可按深度调频 | 试玩出现"必须回城补货"即失败 | ⚠ 数据路径待核验 |
| **D5** | ~~Infravision 金币预算~~ | — | — | 同上（锚点仍成立 ✓） | 小 | — | — | ❌ **放弃**（作者与复核一致：会**推翻** Dark Expedition 既有 Depth 决策 ✗，代价 > 收益） |

| **D6′** | **深度化的购买阶梯**（保留 D2 的"货币回收"**功能** ✓，替换其"耐久"**机制** ✗） | 花金换更好/更多的货 vs 攒钱买保命补给 | 不买（金币留着 ✓） | `SpawnPremium`（`items.cpp:4537-4550`：`PremiumItemLevel` 随 `lvl` 递增 ✓）、入档 `loadsave.cpp:2670-2671` ✓、商店清单 `stores.cpp:52-54` ✓ | **小（纯曲线）** | 深层解锁的货确实出现、金币支出随深度上升 | 实测显示"后期仍无值得买的东西"（曲线无效）⇒ 未起作用 | ✅ 可做（**只闭合"金币废堆"症状**，不闭合压力缺口） |

**顺序（v4，按第一性复核修订）**：**第 0 章 → D1（先测神殿权重）→ D3（深层房间）→ D2（先 timedemo 实测）→ D4′（数据路径核验后）**；**D5 放弃** ✗
（复核者把 D3 排在 D2 之前：D3 的锚点已核验 ✓ 而 D2 依赖未取得的 timedemo 数据 ⚠）

**待实测两项（不得以未核验数字入规格）**：
1. **core 平均 HP 按带**：复核者测算 L13-16 156-185 / L17-20 45-62 ⚠ —— 我**未能复现**（`monstdat` 列名不符）⇒ 需先跑一次实测，再决定 D2 是否成立；
2. **单机死亡是否有 XP 惩罚 → 已实测：无** ✓（`player.cpp:2672-2792` 死亡例程不含任何经验引用 ✓；全仓亦无"死亡扣经验" ✓）；
3. **`StealPotions` 现状 → 已核验** ✓：注册于 `misdat.cpp:194` ✓，核心代码仅**箱子陷阱**路径（`objects.cpp:2057` ✓），**数据行只在 HF**（`mods/hf/txtdata/missiles/misdat.tsv:84` ✓）⇒ 原 D4（怪物 AI）不成立 ✗，D4′（引导既有陷阱路径）数据路径**仍待核验** ⚠。

### 4.6 证据标准（"合理"与"可落地"如何被证明）

| 命题 | 证据形式 | 不能算什么 |
|---|---|---|
| **合理（目标对齐）** | 与 §1 的可操作目标对齐：**死亡不可逆是否改变"下一次下潜的携带策略"** ✓（机械可观测） | ✗ 任何"更好玩"式断言（协议已禁止） |
| **合理（缺口闭合）** | 审计四问（状态/决策/后果/反制）由"缺"变"有" ✓ | ✗ 仅内部自洽 |
| **玩家可感知** | **直接可见的事件**（重载后装备在死亡地点 ✓、房间守卫 ✓） | ✗ 内部状态（`_pmode`/HP）与长期统计量（并集/占比） |
| **可落地** | 锚点 `文件:行号` ✓ + 成本 ✓ + **无新概念**（禁令 4）✓ + **可失败断言**（"改坏→红"）✓ + 全量门禁 ✓ | ✗ 未核验数字（必须标 ⚠ 并进"待实测"） |
| **判定归属** | 机械断言：死亡状态持久性 / 回滚入口 / 掉落持久 ✓；**预注册实验**：惩罚强度 / 携带策略是否改变 ✓ | ✗ 用机械断言代替口味结论，或反之 |

**准入判据（三条；2026-09-18 由作者提出"内容扩充的关键在于是否有效服务体验"）** ✓
1. **心智模型内**（禁令 4）：它创造的决策能否用 D1 既有知识（位置 / 资源 / 信息）理解？
2. **拿掉它，决策会变少吗**（宪章原判据）：**变少 ⇒ 服务体验** ✓；只是**少跑几趟** ⇒ 是税 ✗；
3. **不引入维护**：它是"**可选择的支出**"，不是"**必须的维护**" ✓。
**分类 ≠ 准入** ✓：Base / Expansion / Depth 决定**怎么实现与门禁**（开关、红线），**不决定准不准入** ⇒ **内容可以准入，条件是挂在某条腿上（§4.8）** ✓。

**硬检验：预先规避检验（Pre-emption test，2026-09-18 由作者一问引入）** ✗
> **每项成本机制必须回答：玩家能否通过预先丢弃 / 转移 / 卸下 / 绕路 / 回城规避它？**
> **能 ⇒ 该机制不构成取舍**（红线 11 不通过）：必须改挂**不可转移维度**，或明确记为**弱效应**并降低优先级 ✓。

**首次应用（D1）**：D1 原设的死亡税＝"携带金的一半" ✓（`player.cpp:354-361`）⇒ **可规避** ✗：战前把钱**丢在地上** → 死亡 → 复活 → 走回**捡起**（层持久 ✓、掉落物为关卡对象 ✓）。
⇒ **D1 必须重定义**（见 §4.5 的三选一）。
**由此得到的通则**：D1 的**可转移资产（金/装备/药水）都可被预先规避** ✗ ⇒ 稳定的成本只能挂在**不可转移维度**：**经验 / 世界状态 / 信息 / 时间**（第 0 章属于时间维度 ✓ 因此不可规避 ✓✓）。

**否证数据（逐条，写死）**：
- **D1**：死亡后仍 **100% 全额带现金下潜**（⇒ 空转）；或**神殿使用率 ≈ 0**（⇒ 无动机）；
- **D2**：timedemo 显示后期仍被数值压倒，且形态改动**无感**；
- **D3**：变成**重复刷房**或职业失衡（红线 9/10/11/14）；
- **第 0 章**：预注册实验显示"惩罚过重导致放弃推进"⇒ 撤销（规格 §6-7 ✓）。

**"第 0 章先行"的当前状态：推论，未证明** ✗
⇒ 必须补一次**反例检验**：论证/实测"**跳过第 0 章、直接做 D3**"是否也能产生持续消耗（若房间选择本身不可回滚，则第 0 章不构成前置 ✗）——该反例在定谳前，本规格的顺序**只能视为最佳推断** ✓。

### 4.7 主线行动方案（由 §1.2 的资源循环推出：**四个失败点 → 四条腿**）

> **本表才是"该做什么"的答案** ✓；§4.5 是候选登记簿（机制池），**顺序与取舍以本节为准** ✓。

| 腿 | 闭合的失败点（§1.2(4)） | 机制 | 性质依据 | 依赖 | 成本 | 验收（可失败） | 反证（撤销） |
|---|---|---|---|---|---|---|---|
| **A 需求端** | ① 战力↑ ⇒ 每次交战消耗↓ | **① 战斗内消耗的深度化**：按深度指定"交战形态"（构成 / 空间 / 群压 ⇒ 更长、更多次、更被迫的交战）——**已立项为独立规格**：`2026-09-18-pressure-shapes-design.md`（草案 v1，待作者人工审计 ✓） | **P2**（战斗内已消耗 ⇒ 不可规避 ✓）；**加在需求端** ✓ | **无**（与第 0 章**相互独立** ✓） | 中（复用名册/小队 + 采样守卫机器 ✓） | 逐层 × 职业的形态表（红线 14）+ 既有采样/占比/组合数守卫**全量**；"改坏构成 ⇒ 红" | 实测显示深层交战消耗未变（形态无感）⇒ 未闭合 |
| **B 供给端** | ② 掉落价值↑ 而**金币出口不随深度增长** | **D6′ 深度化购买阶梯**（商店供给随"最深推进"解锁） | **原则 5**（可选择的支出 ✓，非维护 ✗） | 无 | **小（纯曲线）** | 深层解锁的货确实出现；金币支出随深度上升 | 后期仍无值得买的（曲线无效）⇒ 未闭合 |
| **C 绕行通道** | ③ 回城便宜 ⇒ 压力被**"多跑一趟"吸收** | **② 补给通道成本深度化**（**每次使用即消耗**，囤积无效 ✓；机制备选：D4′ 陷阱式补给压力 ⚠） | 原则 2/3 ✓ + 预先规避检验（必须"使用即消耗"，不能靠"携带稀缺" ✗） | **A 之后**（否则只是加税 ✗） | 中-大 | 回城次数/层**不再随卖货单调增长**；压力不被绕行吸收 | 玩家仍靠多跑趟摊平 ⇒ 未闭合，应撤 ✗ |
| **D 赌注** | ④ **第 0 章前物质损失可回滚** | **第 0 章（存档契约 ✓ 已批准/已计划）** + **④ 世界状态作为不可逆后果**（D3 深层房间/守卫） | **P5** ✓ | **第 0 章 → ④** | 中（计划已就绪） | 已列（§6 1-5 + 死亡态重载断言 ✓） | 已列（预注册实验撤销规则 ✓） |

**各腿的使能件（§4.8）**：**A ← C1**（词缀↔需求耦合）；**B ← C2**（Wirt 未知货）；**D 的 ④ ← C4**（unique 地点化）；**"缺失种类 #2" ← C3（情报卷轴，XP 支付）＋ C5（elixir 永属性交换 ✓ 既有内容）**；**C 腿暂无使能件** ✓。

**不在主线内的项（诚实划界）**：
- **③ XP→情报**：**不闭合上述任何失败点** ✗ —— 它增加的是"信息深度"（D1 承重结构之一 ✓），属**独立深度条目** ✓，**移出循环闭合主线**，待主线落定后单独立项 ✓；
- ~~D6 耐久/修理~~ ✗ 已否决（§1.2 原则 5）；
- ~~D1 死亡赋税~~ ⚠ 仍待重定义（属 D 腿的**弱效应**候选 ✓）；
- ~~D5 Infravision~~ ❌ 放弃 ✓。

**顺序（v8）**：**A 与 第 0 章 相互独立 ⇒ 可并行** ✓；**B 最省、可随时插入** ✓；**C 在 A 之后** ✓；**D 中 ④ 在第 0 章之后** ✓。
**对旧说法的修订** ✗：此前"**第 0 章先于一切**"**过强** —— 它只是 **D 腿的前置** ✓；**A 腿（战斗内消耗）与之独立** ✓。

### 4.8 内容准入与"使能件"（C1–C4）

> **内容不是独立轨，而是各腿的使能件** ✓：**压迫（机制）＋ 使能（内容）＝ 一条完整的腿** ✓。

| # | 使能件 | 挂在哪条腿 | 内容是否**已在**（锚点） | 成本 | 验收（可失败） | 反证 |
|---|---|---|---|---|---|---|
| **C1** | **词缀 ↔ 需求耦合** | **A** | ✅ `item_suffixes.tsv:66-69`（`light` +2 / `radiance` +4 / `the dark` −3 / `the night` −2 ✓）、`item_prefixes.tsv:54-58`（抗性系 `Blue…Sapphire` ✓）、`:69-71`（`MANA`/`MANA_CURSE` ✓） | **极低** | 层段**指定需求**（暗处 / 伤害类型）× 词缀池断言（每层段**至少 2 种可准备的反制** ✓，**必须排除 `*_CURSE` 族** ✗）；守卫**可自动化**（静态扫描 `GetMissileType` 的 `case` + 各 `XxxAi` 的 `StartRangedAttack` 调用 ⇒ AI→导弹→伤害类型 ✓ `monster.cpp:1903`）；"改坏构成 ⇒ 红" | 试玩显示玩家**仍不**带针对性装备（耦合未被感知）⇒ 未生效 |
| **C2** | **Wirt 未知货深度化** | **B** | ✅ `stores.h:96-98`（Wirt 所售物品**等级有跟踪** ✓） | 低（纯曲线） | 随机货等级随"**最深推进**"上升的断言 | 玩家从不买 Wirt 的货 ⇒ 未生效 |
| **C3** | **情报类卷轴**（**XP 支付** ⇒ 不可规避 ✓） | **"缺失种类 #2"** | ✅ 动词已在：`IDI_IDENTIFY` / `IDI_PORTAL` / `IDI_RESURRECT`（`itemdat.tsv:28/29/36` ✓） | 中 | 情报必须与**真实层数据一致**（复用名册 ✓）；XP 支付**不可规避**（XP 不可丢弃 ✓） | **从不用**（太贵）或**必用**（太便宜）⇒ 都判失败 ✓ |
| **C5** | **Elixir 的可获得性与深度化**（**永久属性交换**：金 → 永久属性 ✓ 既有内容 ✓） | **"缺失种类 #2"** | ✅ **已在**：四属性 elixir（`itemdat.tsv:89-92` ✓，价值 5000 ✓）+ Golden/Spectral Elixir（`:17/22` ✓）+ Oils（`:85-88` ✓） | 低-中（供应/价格的深度化） | elixir 的**可获得性**随推进上升的断言；**属性总增量不得超出既有内容范围**（红线 9 ✓） | 玩家**从不买/不用** elixir（＝摆件）⇒ 未生效 |
| **C4** | **unique 地点化**（房间 / 事件） | **D（④）** | ✅ `themes.cpp:349/913` + `PlaceGroup`（`monster.cpp:3054`）✓ | 中 | 每层 unique 与位置/房间绑定的断言；重载后状态一致 | 玩家对 unique **位置无记忆**（无感知）⇒ 未生效 |

**v9 两项 ⚠ 已查清（v10）**：
1. **D1 存在"永久增益药剂"** ✓✓：`IDI_GLDNELIX`（Golden Elixir ✓）、`IDI_SPECELIX`（Spectral Elixir ✓）、**四属性 elixir**（力量/魔法/敏捷/体力，价值 **5000** ✓，`itemdat.tsv:89-92` ✓）；另有 **Oils**（Blacksmith Oil / Oil of Accuracy / Oil of Sharpness ✓）。⇒ 我此前"检索无命中"**是错的** ✗（我按 `IDI_ELIXIR` 检索；实际 id 为 `IDI_GLDNELIX`/`IDI_SPECELIX`，而四属性 elixir 的 **id 列为空**、以 `miscId` 索引 ✓）。
2. **空 id 行来源已解释** ✓：它们是**扩展条目**（按 `miscId`/`spell` 索引的 elixir / oil / scroll 等 ✓，如 `:79-95` ✓），**不是脏数据** ✓。

## 5. 红线检查

### 通用红线
| # | 判定 | 依据 |
|---|---|---|
| 1 | 分类 | Depth（§2） |
| 2 | 具体症状 | §3.1（单机死亡无提交 ⇒ 可撤销） |
| 3 | 数值有出处 | §3 每条带锚点；未核验者已标注 ⚠ |
| 4 | "已实施"定义 | 状态为草案 |
| **5（v2 移入）** | **实现不得新增数值倍率** | 由 §4.4 移入（原 §6-7 是静态 grep 伪判据 ✗） |

### 深度层支柱
| 支柱 | 判定 | 依据 |
|---|---|---|
| **DP1** 内生 | ✓ | 存档/死亡/掉落/尸体/持久层全为既有概念；契约不引入新名词 |
| **DP2** 有反制 | ✓ | 第 0 章不新增"内容"，兑现既有代价；§4.3 每行为须各自指名反制 |
| **DP3** 取舍非跑腿 | ✓ | 死亡留痕产生"是否回去捡"的取舍；**不产生回程税** ✓ |
| 全层段定义 | ✓ | 契约作用于全流程；§4.3 各条覆盖 1-16／17-20／21-24 |
| 近战/远程分别评估 | ⚠→✓ | 第 0 章对两职业等价 ✓；④ 必须**逐层×职业**列表（见 §6-6） |

## 6. 验收标准（v2 全部重写：每条必须能写出"改坏哪一条 → 它变红"）

| # | 判据 | 变红条件（必须可写） |
|---|---|---|
| 1 | **死亡代价按死因分类**（怪/陷阱 ⇒ 掉装备+金币；玩家杀 ⇒ 仅耳朵；已有耳朵 ⇒ 无新掉落；镇上死亡 ⇒ 掉金/物；多人 L16/竞技场 ⇒ 不掉） | 改坏 `DeadItem`/`dropItems`/耳朵分支的任一分支 ⇒ 红 ✓ |
| 2 | **死亡提交可观测**：死亡后**重新加载**（不是 mtime ✗）得到：`_pmode=PM_DEATH`、HP=0、装备在**死亡地点**、`_pLvlVisited` 与关卡数据已持久 | 跳过死亡提交 ⇒ 红 ✓；仅写关卡数据不写死亡态 ⇒ 红 ✓ |
| 3 | **回滚入口可枚举**：列出应用内全部加载入口，逐条断言"面对已提交死亡态的存档**不得**呈现死前状态"（**故意移除守卫 ⇒ 必须红** ✓ 取代 v1 的"不存在路径" ✗） | 任一入口被移除守卫 ⇒ 红 ✓ |
| 3b | **切层提交语义**：进入/离开关卡后重载 ⇒ 当层进展（`_pLvlVisited`、已清场/已开门状态、掉落物）仍在 | 去掉 `pfile_save_level`（`interfac.cpp` 单机分支）⇒ 红 ✓ |
| 4 | **掉落持久**：死亡 → 离开该层 → 重进 ⇒ 掉落仍在 | 改坏 `_pLvlVisited`/持久物品序列化 ⇒ 红 ✓ |
| 5 | **提交失败的兜底**：注入"意图已写、存档未替换"的中断与写失败（含 MPQ 删后写失败 `mpq_writer.cpp:154` 与 UNPACKED 截断失败 `pfile.cpp:583-596`）⇒ 启动时**不得**提供死前 Load | 去掉意图检查/原子替换 ⇒ 红 ✓ |
| 6 | **④ 的近/远程分别评估**：附**层段 × 职业 × 阈值**表，每行一条断言（沿用既有采样守卫**并逐行指名**） | 任一行的阈值/守卫被改坏 ⇒ 红 ✓ |
| 7 | **产品实验（移出工程验收）**：死亡惩罚强度属**产品判断** ⇒ 以**预注册实验**执行（N、人群、置信阈值先写死），结果用于"保留/撤销"决策，**不作为门禁** ✓ | 未预注册就下结论 ⇒ 视为无效证据 ✓ |


**范围声明（不设验收）**：§4.1 的 ⑥（外部备份不受约束）、⑦（多人无变化）、⑧（旧档处置）是**边界与兼容性声明**，不作为可失败验收项 ✓；其正确性由实现立项时的兼容性检查覆盖 ✓。

**撤销判据**：若预注册实验显示"惩罚过重导致玩家放弃推进"（阈值与人群在实验前写死），则第 0 章**撤销**，只保留 §4.3 各条 ✓。

## 7. 状态

**草案 v2（2026-09-18）**——待作者复核。复核通过后进入 `superpower-writing-plans`，**且只先做第 0 章**（§4.3 各自单独立项）。

**v8 → v9 修订（作者"内容扩充并非不可能，关键在于是否有效服务于游戏体验"）**：
1. §4.6 新增**准入判据**三条（心智模型内 / 拿掉它决策是否变少 / 不引入维护），并明确 **分类 ≠ 准入** ✓；
2. 新增 **§4.8 内容使能件 C1–C4**（词缀↔需求耦合 / Wirt 未知货 / 情报卷轴 XP 支付 / unique 地点化），每条注明**挂在哪条腿**、内容是否已在（锚点 ✓）、成本、可失败验收与反证 ✓；
3. §4.7 增加**使能件挂接**（A←C1、B←C2、D④←C4、种类#2←C3）✓；
4. §1.2 补三处事实：**装备门槛列存在**（`minStrength/minMagic/minDexterity` ✓）、**词缀表存在**（84/96 行 ✓）、**升级满血满法** ✓。

**v7 → v8 修订（作者"回到主线"）**：
1. 新增 **§4.7 主线行动方案**：§1.2 的**四个失败点**与机制一一对应为**四条腿**（A 需求端＝① / B 供给端＝D6′ / C 绕行通道＝② / D 赌注＝第 0 章+④），每腿含性质依据、依赖、成本、可失败验收与反证 ✓；
2. **③ XP→情报 移出主线** ✗（它不闭合任何失败点，属独立深度条目）；
3. **修订"第 0 章先于一切"的过强说法** ✗：它只是 **D 腿前置**；**A 腿与之独立、可并行** ✓；
4. §4.5 降为**候选登记簿**（机制池），**顺序与取舍以 §4.7 为准** ✓（消除两份并列清单 ✗）。

**v4 → v5 修订（作者一问驱动："我把钱丢地上，交互完再捡起来不行吗？"）** ✗✓：
1. §4.6 新增**硬检验「预先规避检验」**：成本若可被预先丢弃/转移/躲开 ⇒ **不构成取舍**（红线 11）⇒ 必须改挂不可转移维度或记为弱效应 ✓；
2. 该检验**当场打回 D1** ✗（金牌成本可"先丢地上再捡回"规避 ✓）⇒ D1 标为 ⚠ **待重定义**（三选一：弱效应记录 / 改挂**经验**（内生先例 ✓ `objects.cpp:2851/2853`）/ 撤下）；
3. 通则：**D1 的可转移资产都可被预先规避** ✗ ⇒ 稳定成本只能挂 **经验/世界状态/信息/时间**（第 0 章＝时间维度 ⇒ **不可规避** ✓✓）；
4. 实测补录：**单机死亡无 XP 惩罚** ✓（`player.cpp:2672-2792` 无经验引用 ✓）。

**v3 → v4 修订（第一性对抗复核 + 我的实地核验）**：
1. §1.1 补**反例检验**：**"D1 空循环"被推翻** ✗（地下城神殿收金 ✓ `objects.cpp:2868/:2983`）⇒ 弱循环而非空循环；但"第七结构缺失"核心仍成立 ✓；
2. §4.5 **顺序修订**为 第 0 章 → D1（先测神殿权重）→ **D3 → D2**；新增 **D6′ 深度化购买阶梯**（附录 C：保留 D2 的**功能**、替换其耐久**机制** ✗——耐久经 §1.2 原则 5 判定为"必须的维护"，非取舍）；**D5 放弃** ✗；原 **D4（怪物 AI 偷药）作废** ✗（`StealPotions` 数据行仅 HF ✓、核心只有箱子陷阱路径 ✓）→ 改为 **D4′**（引导既有陷阱路径，数据路径待核验 ⚠）；
3. 新增 **§4.6 证据标准**：合理/可落地/可感知/否证数据/判定归属，并明确 **"第 0 章先行"是推论、未证明** ✗（需反例检验）。

**v2 → v3 修订（作者批准追加）**：
1. 新增 **§1.1 设计层闭环审计**（六结构 + **第七结构"持续消耗"** × 两阶段 × {状态/决策/后果/反制}）：缺口由"三个并列项"修正为**一个结构缺失**（症状＝金币失去决策/补给失效/威胁被压过），并确认**第 0 章是其前置**（原"先闭合设计 vs 先做契约"是伪二分 ✗）；
2. 新增 **§4.5 设计轨 D1-D5**（每条含取舍/指名反制/落地锚点/成本/验收/反证/状态），顺序：**第 0 章 → D1 → D2 → D3 → D4 → D5（待裁决）**；
3. 明确 **§4.3＝实现轨、§4.5＝设计轨**，共同前置为第 0 章；
4. **未核验项一律标 ⚠ 并进"待实测"**（core HP 按带、`StealPotions` 现状、神殿金币出口），不得以未核验数字入规格 ✓。

**v1 → v2 修订清单（全部来自两路独立复核 + 我的实地核验）**：
1. 删除 v1 的函数级方案（`pfile_update(true)` **在单机是空操作** ✗）；
2. §3 补齐**写档时机全表**（v1 完全遗漏，且把"离开时写档"当既有语义 ✗）；
3. 第 0 章补**提交失败的语义与原子性**（v1 无 ✗）；
4. 补**边界**（外部备份不受约束）、**死亡不终结角色**、**多人无变化**、**旧档处置**、**决策 34/35 引用**；
5. §6 重写：删 3 条伪判据（"不存在回滚路径"、"不新增倍率"、mtime 断言），#6 表格化，产品实验**移出验收**；
6. 范围收严：只写契约点/失败语义/可观测结果 ✗ 不指定函数与测试实现；
7. §4.3 优先级调整：**② 最省先做、④ 并行、① 未给代价机制不做、⑤ 最后**。

自检：占位符（无 TBD）；一致性（§3 事实 ↔ §4 契约 ↔ §6 逐条对应）；歧义（§4.2 判据已改为"如果 X 则 Y + 既有元素 + 对照实验"）。

---

## 附录 A：两路独立对抗复核的对撞（2026-09-18）

### A.1 复核者之间的**冲突**（由我读码定谳）

| 争点 | `subagent-kiro` | `subagent-codex` | **定谳（我核验）** |
|---|---|---|---|
| 单机是否有隐式写档 | "**没有**，唯一入口是手动 `SaveGame()`" | "**有**，`interfac.cpp:339…452`" | **两者各对一半**：单机**切层写关卡数据**（`interfac.cpp` → `pfile_save_level()` ✓），而**英雄完整写档与退出写档确实仅多人** ✓；**`pfile_update` 单机空操作** ✓（codex 对 ✓） |

### A.2 复核者对我方案的攻击（我全部接受）

| 我的主张（v1） | 裁决 | 性质 |
|---|---|---|
| "单机调用 `pfile_update(true)` 即可对齐" | ✗ 空操作（`pfile.cpp:828-830`） | 函数级方案错误 |
| "存档＝暂停（离开写档、回来继续）"是既有语义 | ✗ 英雄写档仅多人 | 把设计当实现 |
| §6 #3「不存在回滚路径」、#7「不新增倍率」 | ✗ 伪判据（结论已定／静态 grep） | 不可失败 |
| §6 #5（3 分钟 / 40% / 永久退出） | ✗ 错误嫁接试玩协议（该协议明文"默认不采"且不写存档） | 方法论错误 |
| §6 #2（mtime/serial） | ✗ 半伪（无关写入亦变） | 口径错误 |
| §6 #6（"沿用"守卫） | ✗ 未列表格与守卫名 | 不可失败 |
| "只写设计面、不实现" | ✗ 自相矛盾（已指定函数与测试方案） | 越界 |

### A.3 被实地否证的复核主张（不采纳）

1. kiro 候选 2（扩大怪物激活半径）：`GetMonsterActivationRadius` **在仓库中不存在**，机制随光照压制**已作废** ✗；
2. kiro："D1 下层不能回城买" ✗（D1 **有回城卷轴**）⇒ 腰带约束范围是"一次下潜之内"；
3. kiro 候选 1 的代价"烧词缀" ✗（`items.h` 只有 `_iMagical`，**无独立词缀字段**）⇒ 需新增概念（撞禁令 4）；
4. codex 引用的行号有偏移 ⚠（如采样守卫行号）⇒ **v2 只引用用例名/函数名，不引用行号**（更稳）。

**核验为真的 codex 锚点**：`pfile.cpp:828-830`、`interfac.cpp` 8 处、`init.cpp:116-118`、`diablo.cpp:865-866`、`gamemenu.cpp:353`、`mpq_writer.cpp:154`、`loadsave.cpp:249-255`、`pfile.cpp:583-596`、`loadsave.cpp:1272/1379-1382`、`monster.cpp:255/318/3054`、`themes.cpp:349/913`、`gendung.cpp:327`、`diablo.cpp:3163` ✓。


## 附录 C：D2 资源体系对照分析（同一方法；**知识性分析，非本仓可核验** ⚠）

> 说明：D2 不在本仓库中，本附录为**领域知识分析**（未逐条核验），用途是**反哺 D1 的候选集**，不作为规格的验收依据 ✓。

| 维度 | D2 | D1（本仓已核验） | 含义 |
|---|---|---|---|
| **循环单位** | **短程重复**（boss/区域 run，~2 分钟） | **一次下潜**（不可逆、持久） | 单位不同 ⇒ 指标不同 |
| **压力指标** | **吞吐效率**（drops/hour） | **能否活着更深** | D2 的压力是"快不快"，D1 是"活不活" |
| HP/MP | **有自然回复** + 药水廉价 | **无自然回复** ✓ | D2 不吃消耗战；D1 吃 |
| **金币** | **携带上限随等级** ✓（不能囤积）+ 修理/赌博/雇佣兵等**多次回收** | 无上限；出口集中城内且不随深度增长 | **D2 的金币永不变成废堆** ✓；这正是 D1 的缺口 |
| **主要资源** | **物品本身**（词缀/镶嵌/符文/套装/unique，组合深度极大） | 物品模型薄（`items.h:204` 无独立词缀字段 ✓） | D2 的进度＝物品；D1 的进度＝等级 |
| **需求端如何加码** | **按"类型"加码**：免疫/抗性惩罚/区域等级 ⇒ 换装与配装**重新成为决策** ✓✓ | 需求端只随战力**相对下降** ✗ | **最值得学的一条** |
| 死亡代价 | 扣当级进度（**不降级** ✓） | 无 XP 惩罚 ✓；物质损失（第 0 章前可回滚 ✗） | D2 的赌注**有界** |
| 技能/属性 | 原版**不可洗点** ⇒ 玩家强烈反弹 ⇒ 后续加入洗点 | 不可洗点 ⚠ | **承诺无救济＝摩擦，不是深度** |
| 移动/回城 | 传送卷轴廉价 + 传送点 | 回城卷轴（同样廉价） | 都**不是**资源 |
| **压力梯度是否持续** | **是**（类型化需求 + 物品组合深度 + 金币回收） | **否**（需求衰减 + 供给过剩） | D2 成功的关键**不在"刷"，而在"需求端持续被重新指定"** ✓ |

**结论（三条）**：
1. **D2 的成功不是"刷子"本身**，而是四件：**类型化的需求加码** ✓、**金币有上限与多次回收** ✓、**物品作为主资源的组合深度** ✓、**有界赌注（不降级）** ✓；
2. **D1 不能移植的**：刷子式循环单位 ✗（会毁掉"一次不可逆下潜" ✓）、装备/镶嵌/符文体系 ✗（新概念，撞禁令 4；且物品模型薄 ⇒ 需要新建整套系统 ✗）、自然回复 ✗（会杀掉 D1 的消耗战）；
3. **D1 可移植的是"功能"，不是"机制"** ✓：D2 的耐久/修理承担的功能＝**让货币持续被回收** ✓（D2 的金币不会变成废堆 ✓）；但**耐久机制本身在体验上是纯 debuff** ✗（作者判断，且经 §1.2 原则 5 判定：它是"必须的维护"而非"可选择的支出";可用备用件规避;需要"永不磨损"来对冲＝设计异味）⇒ **保留功能、替换机制** ✓：
   **D6′＝深度化的"购买阶梯"**：让商店供给**随推进深度**解锁更高 item-level（现状：`SpawnPremium` 的 `PremiumItemLevel` 仅随角色等级 `lvl` 递增 ✓ `items.cpp:4537-4550` ✓，且已入档 `loadsave.cpp:2670-2671` ✓）⇒ 金币在后期**重新有用途** ✓、**无衰减** ✗、**无维护** ✗、**纯曲线改动** ✓、零新概念 ✓。
   **诚实划界**：D6′ 闭合的是"**金币变成废堆**"这一症状 ✓，**不是**"压力随进程衰减"这个缺口 ✗（后者属候选 ① ✓）。


## 附录 B：**候选实现链**（非规格内容；仅供实现立项参考）

> codex 提出的最小链（我核验了关键前提：`pfile_update` 单机空操作 ✓、死亡态存于 game data ✓）：
> ① 让单机的死亡提交走**单机可用的完整保存**（而非只看多人的 `pfile_update`）；② 提交必须 `writeGameData=true`（死亡态与掉落物在 game data ✓）；③ **只在本地、仅单机**触发（避免远端重复 ✓）；④ 新增可注入中断/失败的测试并注册（`CMake/Tests.cmake` ✓）。
> **注意**：以上为实现候选，**不是本规格的要求** ✗；规格只要求 §4.1 的契约点与 §6 的可观测结果 ✓。