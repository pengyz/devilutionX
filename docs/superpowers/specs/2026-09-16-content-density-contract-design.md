# 内容密度契约 设计规格（Content Density Contract）

**状态**：**已实施**（2026-09-16 批准 v4；2026-09-18 落地并验证：本地全量门禁 ctest **777/777**、漂移 **6/6**、eval smoke 通过；远端 CI run **35328729141** `success` @ `0cec2f43e`）。
**后继**：进入 `superpower-writing-plans` 编写实现计划（`docs/superpowers/plans/2026-09-16-content-density-contract.md`）；本规格批准即锁定判据与参数组，实现阶段不得静默降级。
**分类**：Expansion（§2 判定树第一步——触及 TSV 数值字段；无例外条款、无开关）
**范围**：判据（密度四轴）、度量断言落地、地狱加密度的手段（非对称 cap）、R4 与红线 14 的重推、以及随之而来的**既有契约改写清单**
**上位文档**：宪章 `2026-07-27-better-d1-design-charter.md`；逐层名册规格 `2026-09-15-level-rosters-design.md`（含附录 E）

---

## 1. 问题陈述

**症状（可复现）**：逐层名册功能声称以"内容密度"为收益，但**判据只钉了防御性的"远程占比 ≤ 基线+5pp"**，于是裁决 R4（"期望失配只许改数据、不许放宽阈值"）在实践中被滥用成**砍内容**：

- 地狱段**跨局总内容量下降**：改动前记录"后期并集 15-25 种" → 现 **L13 9 / L14 16 / L15 8**（200 seeds）；
- **曝光率低**：L13 候选 15 种，200 seeds 只见 9 种 → **60%**（6 种"设计了但见不到"）；
- **L17 每 seed 组合数恒为 1**（尾池恰 2 条且同类、L17-24 无 cap）→ 第二次进该层就毫无新内容；
- 而**"内容密度"本身没有任何断言**：P0-A（单局类型数/AI/类别）与 P0-D（并集）**只是报告**，谁都无法在回归时拦住它的下滑。

**结论**：我们缺少**一等的评判判据**与**可失败的守卫**；已有的守卫（占 ceiling）会自动压制密度，且没有任何机制阻止"以密度换达标"。

## 2. 分类判定

| 步 | 判定 | 结果 |
|---|---|---|
| 第一步 | 触及 TSV 数值字段（`level_roster*.tsv`，及其派生的 cap 语义） | **Expansion（无例外条款）** |
| 无开关原则 | 密度契约是判据/数据层改动，玩家安装即生效 | 不提供开关 |

## 3. 事实基础

| 事实 | 数值 / 结论 | 出处 |
|---|---|---|
| 地狱 cap 是**对称**的 | `BehaviorClassCapForLevel`：L13-16 **任意类 ≤2**；L9-12 仅 kite ≤2；其余 0（无上限） | `Source/tables/level_roster.cpp:152-160` |
| 行为类别映射（真实） | Melee/Zombie/Fat/Rhino/Mega/Snake/GoatMelee→Melee；SkeletonRanged/GoatRanged/Succubus/Counselor→RangedTurret；Magma/Storm/Acid/BoneDemon→RangedKite；Fallen→Rally；Bat→Charge；Sneak→Sneak；SkeletonKing→Summon；**default→Boss** | `Source/tables/monstdat.cpp:483-515` |
| **地狱池的唯一非远程类是 Melee** | L13：候选 15（**Melee 9** / Turret 3 / Kite 3），core = Melee×2；L14：15（Melee 8 / Turret 5 / Kite 2），core = Melee1+Turret1；L15：9（Melee 4 / Turret 5 / **Kite 0**），core = Melee2+Turret1 | 本轮实读 `monstdat.tsv` + `level_rosters.tsv` |
| **对称 cap 下地狱单局种类上限 ≈4-5** | 非远程最多 = Melee 2（core 已占满）+ Golem 1 = 3；要凑 6 种且远程 ≤27.8%（L13 ceiling）非远程须 ≥4.3 种 → **数学上不可达** | 由上两行推导 |
| 单局种类（生产路径，40 seeds） | 改动前：教堂 L1-4≈7.0、洞穴 L9-12 **3.3-3.6**、地狱 L13-15 **3.2-3.3**；现在：教堂 7.0/12.0/10.8/10.0、洞穴 **8.0**、地狱 **4.0/5.0/5.0** | 规格 §1（改动前）+ P0-A（现在，用例调 `GetLevelMTypes()`，`SetUpTestSuite` 已 `LoadLevelRoster()`） |
| 跨局并集（200 seeds） | 现在：L13 **9**、L14 16、L15 8（改动前记录"后期 15-25"） | P0-D |
| 既有守卫会被本规格改写 | `HellL13/14/15SameClassTailBaseline` 的 `EXPECT_EQ(tail, 0.0)` **正是对称 cap 的产物**；`RosterQuotaAllowanceIsBinding` 的"饱和层"前提依赖 cap=2（现由 L15 承担） | `test/sampling_behavior_test.cpp` / `level_roster_baseline_test.cpp` |
| 单一真相源已就位 | 加载期 core≤cap 与 floors 可满足性**共用** `BehaviorClassCapForLevel` | `level_roster.cpp:292/335` |
| **独立复核结论** | §3 的池子/core/cap 逐项被独立复算一致；但 **§4.3 的"非对称 cap 即可加密度"被证伪**（见 §4.3 的可行性扫描） | 本轮 `subagent-kiro` 复核（`density-spec-review.md`） |
| R4 的原始措辞 | "B1 既有期望失配 → 调名册或 `class_floors`，**不得**放宽阈值" | 台账 R4 |

## 4. 方案

### 4.1 判据：内容密度四轴 + 不变量

| 轴 | 定义 | 地板 | 目标 |
|---|---|---|---|
| **D1 单局种类** | 每层每局 realized types | **= vanilla 逐层实测值**（**导出方式见下方注**） | **地狱 L13-15 ≥6（硬目标）** |
| **D2 单局威胁多样性** | distinct **AI** / distinct **行为类别** | = vanilla | — |
| **D3 跨局总内容量** | 200 seeds 类型并集 | = vanilla（分层实测，不取区间） | — |
| **D4 曝光率** | 并集 ÷ 候选池 | **≥75%**（**本轴在 L13 从不是瓶颈，见下方更正**） | — |

**vanilla 地板的导出方式（自检补漏 1）**：§3 只记录了**分带区间**（教堂 L1-4≈7.0、洞穴 3.3-3.6、地狱 3.2-3.3），**不足以**当逐层地板。逐层 vanilla 值必须由**空名册夹具**实测导出——即"不加载任何 `level_rosters.tsv` 行"时的行为（这正是 R28 的 legacy 路径：无 params 行 → 预算 4000、尾池不设上限），逐层给出精确值并写明 seed 数；**精度要求（v2，独立复核）**：夹具必须是**物理删空**的名册表（`level_rosters*.tsv` 为空表），而**不是**只省 params——因为 core 预加会改变类型数，两者不等价；**禁止**把分带区间当逐层地板。HF 层段同法（HF 载入 + 空名册）。

**瓶颈更正（v2，独立复核实测）**：曾以为冲突在"D4 曝光率 vs 远程守卫"——**错**。200 seeds 扫描显示 **D3=vanilla(15) 与 D4≈100% 在 tail_draw≥3 时恒满足**，真正的瓶颈是 **D1（单局种类≥6）与"远程占比 ≤ vanilla+5pp"之间的直接冲突**。

**不变量**：①**任何层不得确定性**（每 seed 组合数 ≥2，覆盖 **L1-24**）；②unique base 可达性不降（9c）；③小队形成率 floor 保持（0.95）；④**除本规格明确改动的层外，L1-16 既有读数逐位不变**。
**判据红线**：**D1-D4 任一低于地板即失败**；**不得以"过了压力守卫"为由牺牲任一轴**。

**为何 D4 不取 vanilla 为地板（自检补漏 2）**：vanilla 靠随机采样，200 seeds 的并集≈整个候选池 → 曝光率接近 100%；而名册把抽取**收窄**到 core+尾池，曝光率必然下降。若强行要求 D4 = vanilla，则等于要求"几乎抽遍候选池"——这会与远程守卫（§4.4）直接冲突（抽得越多，远程类越多）。故 D4 取**绝对下限 75%**，并在收尾时用实测确认可达；**若某层实测不可达，必须在规格里写明该层实测值**，不得静默放行。

### 4.2 度量落地：把"报告"升为"断言"（可失败）

1. `ContentDensityWithinVanillaFloor`——按层断言 D1/D2/D3 ≥ 地板（地板常量取改动前实测并注明出处）；
2. `ContentVisibilityFloor`——断言 D4 ≥ 地板（**新增**，直接守"设计了见不到"）；
3. `RosterPerSeedVariety` **扩到 L1-24**（HF 资产门控；L17 现为 1 组合，本规格须整改后才可能绿）；
4. **D7（每层怪物量）暂不进契约**：现有数字的聚合口径与 `MaxMonsters=200` 不自洽，**口径未定义前不许进契约**（附录 B）。

### 4.3 手段：非对称 cap（本规格唯一改生产逻辑处）

| 项 | 内容 |
|---|---|
| 规则（**v4 采纳，引擎实测通过**） | **仅 L13-15**：非远程类（Melee 及任何 Rally/Charge/Sneak/Summon/Boss）**免 cap**；远程类（RangedTurret/RangedKite）cap **2 → 1**。**L16 保持旧约束（任意类 ≤2）**；L9-12 的 kite≤2 **不动** |
| **引擎实测参数组（SPIKE-2，已还原）** | 上述 cap 变更 **＋ L13/L14 `tail_draw` 1/2 → 4**（L15 原 3 已足）→ **L13 种类 4.0→7.0、L14 5.0→7.0、L15 5.0→6.0**；**placed 远程占比 L13 0.171738 / L14 0.267906 / L15 0.200714**，全部 ≤ 各自 ceiling（0.277515 / 0.608760 / 0.633495）；**L12 对照逐位不变**；`PlacedClassMixWithinBaseline` 全层 **PASSED** |
| **新约束（SPIKE 发现）** | cap 变更**必须限定 L13-15**：L16 的出厂 core = 2 Melee + **2 Turret**（CABALIST/ADVOCATE），若把远程 cap=1 也套到 L16，**加载期校验会拒绝启动**（引擎实测报错原文见复核文件）。L16 是"只登记 + 运行时硬编码提前 return"，其数据校验保持旧约束 |
| **可行性扫描（独立复核的模型估计；已被实测部分修正）** | `cap_ranged=2`（原 §4.3）：`tail=3` → D1=5、share=0.2860（超 0.2773）；**`tail=4` → D1=6 但 share=0.3092（超）**；tail 继续增大 share 恒在 **0.28~0.34**，从未回落 → **D1≥6 与远程 ≤vanilla+5pp 在 L13 不可兼得**。`cap_ranged=1`：`tail=4` → **share=0.2558 ≤ 0.2773、D1=6、D3=15、D4=100%** → **唯一可行区间** |
| **SPIKE 引擎实测（v3，纯数据、已还原）** | 临时把 L13 `tail_draw` 1→**4**：单局种类 **4.0 → 7.0**（D1 可达）；但同口径下 **placed 远程占比 0.236 → 0.535983**，ceiling **0.277515** → **远超**。L14/L15 逐位不变；改动已还原、MPQ 已重建 |
| **机制补充（v3）** | 复核者模型预估 0.31-0.34，实测 **0.536**：因为抽到的是**类型**、落地是 **3-5 只一组**，尾抽的远程类型以**整组**计入 placed 口径 → **"类型占比"与"数量占比"之间存在放大**。因此 D1（类型轴）达标**不代表** count 轴安全，两者必须**分别**设守卫 |
| **对照：只用单旋钮都不行（实测）** | ①只抬 `tail_draw`（cap 不变）：L13 种类 7.0 但占比 **0.536** ✗✗；②只改 cap（`tail_draw` 不变）：占比降到 0.097 ✓ 但种类仍是 4.0/5.0 ✗ → **两旋钮必须同时动**，这解释了"为什么单靠放量非远程 cap 不够" |
| 为何"收紧远程 cap"不违背原裁决 | 作者选定的是"**非远程放量、远程受压**"（A）而非"抬远程压力"（B）。把远程 cap 从 2 **收紧到 1 是进一步压低远程压力**，方向与 A 一致；代价是**该层段远程种类上限降到 1**（core 中的远程仍存在，因 core 不受 cap 约束） |
| 仍未采用 | (c) 扩池（新增非远程怪，本次范围外）；(d) 抬远程占比红线（=B，作者已排除）。若将来要多于 6 种且不触 cap，唯一路径是 (c) 或"类别感知的抽取偏好"（后者属新增机制，须按红线 10 指名反制） |
| 单一真相源 | 只改 `BehaviorClassCapForLevel`；加载期 core≤cap 校验与 floors 可满足性自动跟随 |
| 契约外溢（**必须显式改写，不是放宽**） | 见附录 A：①`HellL13/14/15SameClassTailBaseline` 重推；②`RosterQuotaAllowanceIsBinding` 的饱和层前提重推；③B1 契约文档与阶段 A/B 验收表的语义标注 |
| 不动 | 玩家交互（零新增 UI/按键/概念）、`kRangedShareTolerance`、`kSquadFormationFloor`、L1-12 与 L16 既有数据 |
| **红线 10 更正（v2）** | 原文称"本规格不新增压力故不需新反制"——**在 L13 不成立**：非远程池有限（Melee 9），当抽取逼近池子极限时**剩余抽取被结构性推向远程类**，远程**数量**仍会上升。因此本规格压住远程侧（cap=1）**才**使"不新增压力"成立；若某层仍出现远程侧上升，则必须按红线 10 **指名反制**（具体装备/消耗品/行为）后方可实施 |

### 4.4 R4 与红线 14 的重推

- **R4 反转**：新规则为"**不得以牺牲 D1-D4 任一轴为代价达标**"。冲突时只允许 ①扩池 ②重推守卫 ③**若必须砍，须在规格中明写损失量与理由**。
- **红线 14 新表述**："**远程占比不得高于 vanilla 同层**；当某层因加密度而使远程侧上升时，必须有**指名的玩家可执行反制**（红线 10）。"——原 `基线+5pp` 保留为**报警线**，但**不再作为砍内容的许可证**。

### 4.5 范围外（明确不做）

不动玩家交互与 UI；不动战斗数值；**不做** HF 的 10 个 AI 重分类（附录 B 的 like-for-like caveat 保持，留作阶段 C）；**不引入新怪内容**（C 方案本次不做）。

## 5. 红线检查

### 通用红线
| # | 判定 | 依据 |
|---|---|---|
| 1 | 分类 | Expansion（§2） |
| 2 | 具体症状 | §1（并集下降 15-25→9/16/8、L13 曝光率 60%、L17 确定性） |
| 3 | 数值有出处 | §3 每行 `文件:行号` 或本轮实测 |
| 4 | "已实施"定义 | 状态为草案；新增断言由生产夹具驱动 |

### 扩充层红线
| # | 判定 | 依据 |
|---|---|---|
| 9 | 非膨胀数值 | 只改**类别上限**与数据；不加数值、不改伤害/HP/掉落 |
| 10 | 指名反制 | 本规格**不新增压力**（远程侧 cap 不变），故不需要新反制；若某层远程侧因密度上升，按 §4.4 必须指名反制后才允许 |
| 11 | 取舍非负担 | 不改玩家规则、不新增必记概念；密度是**感知收益**而非新规则 |
| 12 | 全层段定义 | 契约覆盖 L1-24（HF 由资产门控的断言覆盖） |
| 13 | 与既有系统互动 | 直接改 B1 的 cap 语义（单一真相源）并与名册/floors/9b/9c 联动 |
| 14 | 近战/远程分别评估 | 本规格的**核心**：远程侧 cap 与 ceiling 不动；非远程侧单独放量，并逐层重测占比 |

### 禁令
1 未实现内容标注状态 ✓；2 无不可落地表格 ✓（D7 明确排除）；3 数值有出处 ✓；4 不新增玩家必记概念 ✓；5 红线逐条给依据 ✓；6 不用占位/永真测试 ✓（新断言均要求反证）；7 不改行尾 ✓

### 固定问句
**本改动是否改变任何玩家可见的标识 / 提示 / 着色 / 音效语义？** → **否**（仅类别上限与数据；无 UI 变化）。

## 6. 验收标准

| # | 判据 | 用例 / 证据 |
|---|---|---|
| 1 | D1/D2/D3 ≥ vanilla 地板（逐层） | `ContentDensityWithinVanillaFloor`（HF 资产门控） |
| 2 | D4 曝光率 ≥75%（逐层） | `ContentVisibilityFloor` |
| 3 | 每层每 seed 组合数 ≥2（**L1-24**） | `RosterPerSeedVariety`（扩展后） |
| 4 | 地狱 L13-15 单局种类 **≥6** | **已由 SPIKE-2 实测证明可达**（7.0/7.0/6.0，参数组见 §4.3）；实现后须复测确认 |
| 5 | 远程占比 **≤ vanilla 同层**（报警线：≤基线+5pp） | `PlacedClassMixWithinBaseline`（含 HF 层段） |
| 6 | unique base 可达性不降（9c） | `HellUniqueBasesRemainReachable`（扩展后） |
| 7 | 小队形成率不降 | `SquadFormationRate` / `ShippedSquadChanceRealisesSquadsOnEveryLevel` |
| 8 | 既有 L1-16 读数逐位不变（除明确改动层） | `PlacedClassMixReport` / P0-A 对账 |
| 9 | 每条新守卫**可失败**（反证：改坏→必红→恢复→绿） | 报告内联证据（RA6） |
| 10 | 门禁 + eval + CI | `run_tests.py`（含漂移 F）、`eval --smoke`、CI `-R pengyz/devilutionX` |

## 7. 状态

**草案 v1（2026-09-16）**——待作者复核。复核通过后置「已批准」，并进入 `superpower-writing-plans`。
自检：占位符扫描（无 TBD/TODO；D7 显式排除而非留白）、内部一致性（§4.1 判据与 §6 验收逐条对应）、范围（本规格只覆盖判据+cap+重推；数据整改/断言落地成为下一步计划的任务）、歧义（"硬目标"已明确为"达不到须回作者改判"，见附录 C）。

---

## 附录 A：既有契约改写清单（本规格的外溢成本）

| # | 契约 | 现形态 | 为何必须改写 | 改写成 |
|---|---|---|---|---|
| A1 | `HellL13/14/15SameClassTailBaseline` | `EXPECT_EQ(tail, 0.0)` | 它是**对称 cap**的产物；非对称后"非远程 tail"合法 | 远程类 tail ≤2（不变）；非远程 tail 受 roster/`tail_draw` 约束（给出新上界与实测） |
| **A2（已执行）** | `RosterQuotaAllowanceIsBinding` | 依赖"存在贴限饱和层" | **已核实**：饱和由 **L15 的 Melee core（BALROG+GSNAKE = cap2）** 触发 → 非对称后 Melee 的 cap 变 0，`if (cap==0) continue;` 直接跳过 Melee → 该层退出饱和列表，**`ASSERT_FALSE(saturated.empty())` 有变红风险** | **已执行**：前提改为"**该层某受 cap 类的候选数 > cap**"（更强，直接证明 cap 在循环里咬住；承载 L13-15 远程类，cap=1）；**注意**：L13 无 Turret/Kite core、L14/L15 各仅 1 个 Turret core → **当前没有任何层的远程类能靠 core 自然贴限** → 该守卫的"binding"性质必须**另建前提**（属本规格的实施项，不是顺手改注释） |
| A3 | B1 契约文档 / 阶段 A-B 验收表 | 描述"任意类 ≤2" | 语义已变 | 标注变更与理由（引用本规格） |
| **A5** | `docs/knowledge/decision_save_format_policy.md` | 提到 cap/名册语义 | 非对称 cap 改变了"同层同类上限"的表述 | 同步描述并标注变更来源 |
| **A6** | `eval/cases/rng/level-rosters.yaml` | 含 cap 相关说明/计数 | 同上 | 同步注释与计数 |
| **A7** | `test/level_roster_test.cpp` 的三条 cap 语义用例 | 断言"任意类 ≤2" | 非对称后 L13-15 近战侧不再封顶，三条全红（实现中实测发现） | **改载体、留机制**：floor/core 超 cap 的载体换成 RangedTurret（cap1）；"按层作用域"用例同样换载体 |
| **A8** | `QuestPreAddRePickDoesNotDoubleCount` | `EXPECT_LE(MaxSameClassCount(classes), 2)` | 同上，断言不再描述生产 | 改为**与 cap 无关的更强表达**（类型列表无重复）＋远程侧 cap 交叉校验 |
| **A9** | `HellUniqueBasesRemainReachable`（9c）的 L13 例外清单与 `checkedUniques==10` 前提 | 3 个 L13 近战 base 记为结构性不可达 | 非对称后被抽到（**可达性提升**），例外清单过期、前提计数失效 | 清空例外清单（保留机制）、前提 10 → **13**；实测 L13 5/5、L14 6/6、L15 2/2 |
| **A10** | **B1 的 caves kite cap**（`BehaviorClassCapForLevel` 的 `L9-12 RangedKite<=2`）及其两条守卫 | cap=2；`CavesKiteTailBaseline`（kite 类型尾=0%）；`CavesAnyClassTailBaseline`（L9 59.93%、L10-12 100% 钉值） | caves 池子 kite 密集且 core 吃满 cap → 5-6 个 kite 类型永不可抽，**并集低于 vanilla**（L10 12<17、L11 11<15、L12 13<16）→ 直接违反契约 D3 地板；另两条 `level_roster_test` 用例把 L10 的 kite cap 写进断言 | 按 §4.4 的 R4 反转**退役该 cap**（并集恢复 17/15/16；份额仍低于 vanilla）→ `CavesKiteTailBaseline` **退役**（类型数垄断已非契约，放置症状由 A4 守卫）并把最终值留档；`CavesAnyClassTailBaseline` 重钉 L9 59.93→**77.20**；两条 validator 用例**改载体到 L14 RangedTurret（cap1）并改名**（`...TheLevelsClassCap`） |
| **A11** | `BehaviorClassCapForLevel` 新增 **L2 远程类 cap=1**；L2/L3/L5/L6/L7 的 core 组成 | 原为无 L2 特例；五层各有 1 个远程 core | A4 主判据（≤ 同种子 vanilla）首跑即发现这五层的 ranged share 高于 vanilla（+0.13~+3.47pp），此前被 +5pp 报警线默许；L2 单靠换 core 仍 +0.16pp，而只加 cap 会让并集 21→20（违反密度优先） | L3/L5/L6/L7 换成近战 core（1:1）；L2 = 换 core + **cap=1** + 补一条近战 core → 两项判据同时满足（占比 0.0361、并集 21） |
| A4 | `PlacedClassMixWithinBaseline` | 绝对 ceiling（基线+5pp） | §4.4 把它降为**报警线** | 主判据改为"≤ vanilla 同层"，报警线保留 |

## 附录 B：测量口径 caveat（不得静默忽略）

1. **HF 分类学缺口**：`GetBehaviorClass` 的 `default→Boss` 把 10 个仅见于 L17-24 的 AI 归为 Boss（其中数只是远程施法者）→ 该层段 `rangedShare` **低估**；本规格保持 like-for-like，**不重分类**（重分类会移动 L1-16 基线前提，属阶段 C）。
2. **D7（每层怪物量）口径未定义**：现有输出（如 L1 的 18585）与 `MaxMonsters=200` 不自洽，**未定义前不进契约**。
3. **并集与种子数绑定**：D3 的并集取 200 seeds；与 D1（40 seeds）不同口径，不得相互推导（曝光率因此是**下界**）。

## 附录 C：回退规则与目标强度裁决

- **回退规则（v2，按复核建议改写）**：若某层无法在"远程占比 ≤ vanilla+5pp"约束下达到 D1 ≥6，**视为该层密度目标与远程红线冲突**，必须由作者在下列三选项中**二选一并记录决策**：**(a)** 该层 D1 目标降级为该层**实测最大可行值**，并同时给出产生它的 `tail_draw`/cap 组合与对应远程占比；**(b)** 该层远程 cap 从 1 再收紧（说明对报警线的影响）；**(c)** 扩池（新增该层非远程候选）。**禁止**只记录一个更低的 D1 数字而不说明走了哪个选项、哪组参数。
- **目标强度**：地狱 ≥6 为**硬目标**；达不到必须在收尾时**回到作者改判**（不允许在实现中静默降级）。理由：本规格的全部动机就是"不能再靠静默降级过守卫"。

---

## 附录 D：实施记录（2026-09-18）

| 任务 | 实测结果 |
|---|---|
| **T1 vanilla 地板** | 用**超范围单行（level 99）夹具**导出（加载器拒绝仅表头文件，`Source/data/file.cpp:53-57`）：types L1-15 = 7.0/6.8/7.1/6.9/6.0/5.5/4.6/4.3/3.6/3.5/3.2/3.4/3.2/3.2/3.2；union = 7/21/25/23/21/17/16/17/17/17/15/16/16/16/10 —— 与名册规格 §1 记录的改动前值一致 ✓ |
| **T2 非对称 cap** | L13-15 远程 cap=1、非远程免 cap（L16 保持 2）；L13/L14 `tail_draw`→4。**单局种类 L13 7.0 / L14 7.0 / L15 6.0**（vanilla 各 3.2）；placed 远程占比 **0.171738 / 0.24515 / 0.208764** 全 ≤ ceiling；unique 可达 **5/5、6/6、2/2**；每 seed 组合数 **295/243/5** |
| **T3 L17** | 组合数 **1 → 3**（删 `MT_PSYCHORB`；对照删 `MT_STINGER` 只到 2 且施法者余量仅 0.42pp）→ 施法者占比 0.169385（余量 9.4pp） |
| **T4 密度断言** | 首跑即抓到 **L10/L11/L12 并集低于 vanilla**（12/11/13 vs 17/15/16）→ 根因是 B1 的 caves kite 类型 cap 被 core 吃满 → 按 §4.4 的 R4 反转**退役该 cap** → 并集 **17/15/16** 恢复；份额 0.462348/0.442413/0.436842/0.40646，**全部低于 vanilla** |
| **A4 主判据反转** | 新 `kVanillaShareBaseline`（同 fixture/同 200 seeds/**同 seed base 9000**）取代"基线+5pp"作主判据，后者降报警线。**历史数组与同种子 vanilla 最大差 14.6pp**（L15 0.4371 vs 0.5835）。首跑抓到 **L2/L3/L5/L6/L7 高于 vanilla**（+0.13~+3.47pp）→ 数据重调后 **L2 0.0361 / L3 0.0607 / L5 0.0737 / L6 0.1317 / L7 0.1428** |
| **D4 退役** | 曝光率实测 >100%（并集含任务预加/Golem/unique base 而候选池不枚举）⇒ 分母不适定；其意图由 D3 并集地板承载 |
| **外溢 A1-A11** | 见附录 A：A1 hell 尾抽守卫改写、A2 定额守卫前提改为"候选数 > cap"、A3-A6 文档/eval、**A7-A9**（cap 语义用例改载体、任务预加用例改表达、9c 例外清单清空）、**A10**（caves kite cap 退役 + 其两条守卫）、**A11**（L2 远程 cap + 五层 core 组成） |
| **验证** | 全量门禁 **ctest 777/777 failed 0 pct 100**、**漂移 6/6 通过**、eval smoke exit 0；`level_roster_test` 41/41、`sampling_behavior_test` 36/36 |

**尚未闭合**：L20/21/22/L24 的"薄但合法"多样性只报告未改（其 ceiling 为 0.05 或受预算剪枝，无零风险增厚空间）。
