# P0 实测：采样预算到底买到了什么（引擎内实测，2026-09-15）

> **性质**：实测数据 + 解读，**不是规格**。为「P0：采样与编组」的决策提供第一手数字。
> **方法**：新增的 test-only 测量套件（`test/sampling_behavior_test.cpp` 的 `SamplingBaselineTest.Measurement*`），**调用真实引擎** `GetLevelMTypes()`，未改任何生产代码。
> **复现**：`cd build && SAMPLING_REPORT=/tmp/p0.md ./sampling_behavior_test --gtest_filter='SamplingBaselineTest.Measurement*'`
> **环境**：本机 MPQ 为 `spawn.mpq` + `hellfire.mpq`（**无 `DIABDAT.MPQ`**）；`gbIsSpawn` 在测试上下文不可靠（见 P0-C）。

---

## P0-A：每层实际生成的怪物类型（40 seeds 平均，精灵预算 4000）

| 层 | 类型数 | distinct AI | distinct 行为类别 | Σ`image` 均值 | Σ`image` 最大 |
|---|---|---|---|---|---|
| 1 | 7.0 | 5.0 | 3.0 | 3889 | 3889 |
| 2 | 7.0 | 5.3 | 4.1 | 3862 | 3997 |
| 3 | 7.0 | 5.3 | 4.1 | 3830 | 3999 |
| 4 | 6.9 | 5.5 | 4.3 | 3839 | 3997 |
| 5 | 6.1 | 5.5 | 4.2 | 3861 | 3991 |
| 6 | 5.4 | 4.8 | 3.9 | 3852 | 3997 |
| 7 | 4.6 | 4.3 | 3.8 | 3765 | 3950 |
| 8 | 4.3 | 4.1 | 3.5 | 3758 | 3978 |
| 9 | 3.6 | 3.4 | 2.8 | 3735 | 3912 |
| 10 | 3.5 | 3.4 | 2.7 | 3762 | 3972 |
| 11 | 3.3 | 3.2 | 2.5 | 3692 | 3972 |
| 12 | 3.4 | 3.2 | 2.6 | 3667 | 3972 |
| 13 | 3.2 | 3.0 | 2.6 | 3624 | 3906 |
| 14 | 3.2 | 3.1 | 2.8 | 3523 | 3906 |
| 15 | 3.3 | 3.0 | 2.7 | 3554 | 3776 |
| 16 | 4.0 | 4.0 | 3.0 | 6506 | 6506 |

**解读**

1. **塌缩是真实的，且比既有诊断更靠前**：教堂 ~7 类 → 地狱 **3.2-3.3 类**；distinct AI 从 5.0-5.5 掉到 **3.0-3.1**；distinct 行为类别 4.3 → **2.5-2.8**。也就是说后期一层里，玩家面对的是**大约 3 种怪、3 套 AI、2-3 类行为**。
2. **Σ`image` 几乎顶到 4000**（后期 3523-3762，教堂 3829-3889）→ **预算是绑定约束**，不是 `MaxLvlMTypes=24`，也不是候选池不够（见 P0-B 的 candidates 列：后期仍有 9-16 个候选）。
3. **L16 是例外**：它走硬编码分支（`GetLevelMTypes` 在 currlevel==16 时预加 Advocate/RBlack/Diablo 后直接 return），预加**不经过预算**，所以 Σ`image`=6506 超出 4000。这解释了为什么"最后一层"反而比 13-15 层多。
4. 与探索阶段的 Python 模拟相比，本表是**引擎实测**：趋势一致（后期 3 类量级），个别层有差异（本表 L2=7.0 而模拟给 5.9），**以本表为准**。

---

## P0-B：给定预算能容纳多少候选类型（按抽取顺序的最好/最坏界）

| 层 | 候选数 | 4000 | 6000 | 8000 | 12000 | 20000 |
|---|---|---|---|---|---|---|
| 1 | 6 | 6/6 | 6/6 | 6/6 | 6/6 | 6/6 |
| 2 | 20 | 4/8 | 8/11 | 11/14 | 19/19 | 20/20 |
| 3 | 24 | 4/8 | 8/12 | 11/15 | 19/21 | 24/24 |
| 4 | 22 | 4/8 | 7/12 | 10/15 | 18/20 | 22/22 |
| 5 | 20 | 3/8 | 5/11 | 7/13 | 12/17 | 20/20 |
| 6 | 16 | 3/7 | 5/8 | 7/10 | 11/14 | 16/16 |
| 7 | 15 | 2/4 | 3/6 | 5/8 | 9/12 | 15/15 |
| 8 | 16 | 2/5 | 3/7 | 4/9 | 8/12 | 16/16 |
| 9 | 16 | 2/4 | 3/5 | 4/7 | 7/9 | 13/14 |
| 10 | 16 | 2/4 | 3/5 | 4/7 | 6/9 | 11/14 |
| 11 | 14 | 1/3 | 2/4 | 4/5 | 6/8 | 11/12 |
| 12 | 15 | 1/4 | 2/5 | 3/7 | 5/9 | 11/13 |
| 13 | 15 | 1/4 | 2/5 | 3/6 | 5/9 | 10/12 |
| 14 | 15 | 1/4 | 2/5 | 3/7 | 5/9 | 10/12 |
| 15 | 9 | 1/3 | 2/4 | 3/5 | 6/7 | 9/9 |
| 16 | 6 | 1/3 | 2/4 | 4/5 | 6/6 | 6/6 |

（每格是**最好/最坏界**：按 image 从小到大 / 从大到小装入，不代表引擎的随机抽取结果；实际落在两者之间。）

**解读**

1. **预算是唯一值得掰的杠杆**：同一层里，4000→8000 让后期从 1-4 类变成 3-7 类；→12000 达到 5-9 类；→20000 时后期几乎吃满候选池（10-14 / 11-14）。
2. **候选池从不是瓶颈**：L3 有 24 个候选、L9-14 有 14-16 个，但 4000 只放得进 1-4 个。
3. **内存代价不构成约束**（2026-09-15 作者判定）：本项目面向现代设备，预算提高一个数量级也无所谓；`image`↔KiB 标定（P0-C）因此**只是遗留目标（3DS/Vita/Amiga）的编译期参数问题**，不再是设计门槛。

**但"提高常量"会换出另一个病——身份同质化**：见上表，预算 20000 时后期层能容纳 9-14 / 9-15 个候选，即"该深度全部可用怪"。层与层之间由此失去身份，塌缩被替换成同质化。

---

## P0-C：`image` 列 vs 真实精灵字节 —— **未决**

测量结果：22 个被实测到的类型，`sprite KiB` **全部为 0.0**，即 `OpenAsset("monsters\\<spritePath><animletter>" + DEVILUTIONX_CL2_EXT, size)` 在本环境没有解析到任何怪物精灵文件。

**未决原因（两种可能，尚未区分）**

1. **本机数据不含多数怪物精灵**：只有 `spawn.mpq`（shareware）+ `hellfire.mpq`，**没有 `DIABDAT.MPQ`**；但连 Zombie/Skeleton/Fallen（spawn 里应当存在）也是 0，所以这不像是全部原因。
2. **路径/扩展名约定与加载器不一致**：测试镜像的是 `monster.cpp:3224` 的 `FileNameWithCharAffixGenerator({"monsters\\", spritePath}, DEVILUTIONX_CL2_EXT, Animletter)`；本构建 `UNPACKED_MPQS=OFF`（`build/CMakeCache.txt`）→ 期望 `.cl2`。**尚未验证** MPQ 内实际条目名（大小写、目录分隔、扩展名）。

**为什么仍然测**：内存不是决策门槛（作者已判定），但该标定对**遗留目标平台**的预算参数化有意义，且能顺带确认加载量级。**它不阻塞任何决策。**
**如何闭合**：用 `tools/mpqextract`（或 debugger/`LogVerbose`）列出 `DIABDAT.MPQ`/`spawn.mpq` 里 `monsters\` 下的真实条目名；或直接在测量套件里枚举候选路径变体（大小写 / 正反斜杠 / .cl2 vs .clx）打印成功项。需要一张 **retail `DIABDAT.MPQ`** 才能覆盖后期怪物。

---

## P0-D：层身份基线（每层 200 seeds 的"名册"取并集）

| 层 | 名册规模 | 与上一层的 Jaccard | 与 L1 的 Jaccard |
|---|---|---|---|
| 1 | 7 | - | 1.0 |
| 2 | 21 | 0.3 | 0.3 |
| 3 | 25 | 0.6 | 0.1 |
| 4 | 23 | 0.5 | 0.0 |
| 5 | 21 | 0.6 | 0.0 |
| 6 | 17 | 0.5 | 0.0 |
| 7 | 16 | 0.5 | 0.0 |
| 8 | 17 | 0.5 | 0.0 |
| 9 | 17 | 0.5 | 0.0 |
| 10 | 17 | 0.5 | 0.0 |
| 11 | 15 | 0.6 | 0.0 |
| 12 | 16 | 0.5 | 0.0 |
| 13 | 16 | 0.5 | 0.0 |
| 14 | 16 | 0.7 | 0.0 |
| 15 | 10 | 0.4 | 0.1 |
| 16 | 4 | 0.1 | 0.1 |

**解读（含对我先前假设的更正）**

1. **更正：层身份不是"缺失"，而是"看不见"。** 我此前（依据 P0-B 的 20000 档位）推断"提高预算会让每层都一样"——那部分成立，但**现状并非"每层相同"**：相邻层 Jaccard 只有 0.5（约共享一半），而与 L1 相比在 L4-14 几乎为 0（完全不同的怪）。
2. **真正的问题是"名册很大、但每局只露出一小片"**：一层在 200 seeds 下的名册有 15-25 种，而单局实际只生成 3.2-7 种 → **玩家一局只见到名册的约 20%**，其余是永远没遇到的"潜在内容"。
3. **两个结论并存不矛盾**：现在=身份**潜在**（预算太小，看不见）；把预算抬到 20000 = 后期层能露出 9-14 / 名册 15-17，相邻层 Jaccard 会趋近 1 → **身份真的消失**。
4. **因此正确的设计目标不是"给层加身份"，而是"让已有的身份可见"**：核心名册（必然出场的招牌怪）+ 轮换尾池（随机的新鲜感）。这与"提高预算"是两条不同的路：前者在固定预算下把可见度从 20% 提到"核心 100% + 尾池随机"，后者只是把随机切片放大。
5. **异常层**：L16 只有 4 种（硬编码分支，且预加不经过预算）；L15 只有 10 种；L1 名册 7 且几乎固定——这三层的名册化设计要与其它层分开处理。

## 对决策的直接含义（2026-09-15 修订：内存不是门槛；"提高常量"不是答案）

**先摆真约束**（都不是内存）：

1. **存档语义是真代价**：`monster.levelType` 是 `LevelMontypes` 索引（`Source/loadsave.cpp:681`），由采样顺序决定；改采样 = **旧存档里怪物种类对应改变**，既有 `MonsterConversionData` 不负责索引重映射 → 这类改动应**一次性、刻意地做**，不要反复微调常量。
2. **确定性与夹具**：层生成 RNG draw-count 变化 → timedemo 等需重基线（该夹具已 quarantine，窗口合适）。
3. **身份同质化是设计约束（经 P0-D 修正）**：现状是身份**潜在**（名册 15-25 种，单局只见约 20%）；把预算抬到 20000 会让相邻层 Jaccard 趋近 1，才真的变成"每层都一样"。设计目标因此是"**让已有的身份可见**"（核心名册 + 轮换尾池），而不是"加预算"。

**因此推荐形态：逐层名册 + 配额（roster + quota）**，而不是提高常量：

| 要素 | 内容 |
|---|---|
| 核心名册 | 每层段固定在场 4-6 种，按行为类别配比（贴身/远程/冲锋/群体基本盘） |
| 轮换尾池 | 该深度余下候选中随机取 2-4 种 |
| 行为配额 | B1 的"同类上限"扩成"下限+上限"（同一处代码 + 同一 harness） |
| 层身份 | 每层段招牌怪 + 招牌行为（教堂=贴身、洞穴=拉扯+条件冲锋、地狱=枯竭+稀有强敌） |

副产品：A1/A3 的变体成为**名册核心成员**后，其效果从"抽奖"变为"必然出场"，验收不再需要降级。

### 原始三条含义（保留）

1. **"多几类怪"在当前预算下买不到**：后期一层只有 3.2 类，Σ`image` 已顶到 3523-3762/4000。加新怪物（image≈1000）会挤掉 1-1.5 个原类型——**与探索阶段 E5a 的"Δ 可能为负"一致，且现在有引擎实测支持**。
2. **固定预算下，唯一还有表达力的轴是"组合"**：既然只有 3 类怪、3 套 AI 进得来，那么"谁和谁一起出现、以什么队形出现"（已实现却未被普通散布调用的 `PlaceGroup` leader/leashed）就是当前**信息量最大**的改造点——它不增加 memory 成本，只改调用点。
3. **A1/A3 的量级预期应按下调**：一层只容 3.2 类，A1 的变体要先进池才可能被看到；A3 改的 2-3 只洞穴风筝还要与 B1 已生效的"洞穴 kite ≤2"竞争。**这不是否定 A1/A3，而是把它的验收改为"变体在采样后仍可见"**（框架 §2 已回写该反向依赖）。

---

## 复现与产物

| 项 | 位置 |
|---|---|
| 测量套件 | `test/sampling_behavior_test.cpp` → `SamplingBaselineTest.MeasurementRealisedPerLevel` / `MeasurementBudgetFitBounds` / `MeasurementImageVsSpriteBytes` |
| 原始数据表 | 运行 `SAMPLING_REPORT=<path>` 时追加写入（本节 P0-A/B/C 三张表即为输出） |
| 未决项 | P0-C（`image` ↔ KiB 标定）；见上节"如何闭合" |