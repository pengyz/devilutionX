id: "combat-dead"
name: "尸体生成与操作"
category: "combat"
difficulty: "basic"
tags: ["corpse"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "dead_test"
  filter: "Corpses.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  尸体生成与操作。
