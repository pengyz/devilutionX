id: "combat-cursor"
name: "光标状态与操作"
category: "combat"
difficulty: "basic"
tags: ["cursor"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "cursor_test"
  filter: "Cursor.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  光标状态与操作。
