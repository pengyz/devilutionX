id: "dark-expedition-light-radius"
name: "暗夜远征：光照半径按层段缩放"
category: "dark-expedition"
difficulty: "basic"
tags: ["depth-layer", "light"]

setup:
  mpq_required: false
  timeout: 30

run:
  backend: "gtest"
  binary: "dark_expedition_light_test"
  filter: "DarkExpeditionLightRadiusTest.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 7
  skipped_max: 0
  output_contains: ["[  PASSED  ] 7 tests."]

description: >
  Hell 60% / Nest 85% / Crypt 50% 倍率，整数百分比数学截断确定性，
  clamp [2,15]，装备加成随倍率缩放，曲线单调。
