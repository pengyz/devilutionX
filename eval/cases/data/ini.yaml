id: "data-ini"
name: "INI 配置读写"
category: "data"
difficulty: "basic"
tags: ["config"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "ini_test"
  filter: "IniTest.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  INI 配置读写。
