id: "render-animationinfo"
name: "动画帧信息"
category: "render"
difficulty: "basic"
tags: ["animation"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "animationinfo_test"
  filter: "AnimationInfo.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  动画帧信息。
