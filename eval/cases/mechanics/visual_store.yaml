id: "mechanics-visual_store"
name: "商店可视化 UI"
category: "mechanics"
difficulty: "medium"
tags: ["stores"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "visual_store_test"
  filter: "VisualStoreTest.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  商店可视化 UI。
