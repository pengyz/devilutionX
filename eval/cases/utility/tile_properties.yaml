id: "utility-tile_properties"
name: "tile 属性"
category: "utility"
difficulty: "basic"
tags: ["utility"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "tile_properties_test"
  filter: "TilePropertiesTest.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  tile 属性。
