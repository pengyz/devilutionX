id: "utility-crawl"
name: "文件遍历"
category: "utility"
difficulty: "basic"
tags: ["utility"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "crawl_test"
  filter: "CrawlTest.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  文件遍历。
