id: "utility-vision"
name: "视野计算"
category: "utility"
difficulty: "basic"
tags: ["vision"]

setup:
  mpq_required: false
  timeout: 60

run:
  backend: "gtest"
  binary: "vision_test"
  filter: "VisionTest.*"

expected:
  exit_code: 0
  failed: 0
  passed_min: 1
  skipped_max: 5
  output_contains: ["[  PASSED  ]"]

description: >
  视野计算。
